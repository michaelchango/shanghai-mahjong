# 机器人接入 CloudBase 大模型 —— 调研与实施方案

> 目标：把牌桌上的机器人从「死代码启发式」升级为「大模型决策」，同时**不破坏现有规则、不拖垮手感、成本可控**。
> 结论：可行。改动面很小（2 个函数 + 3 处调用点），本地启发式保留为兜底，所以**大模型挂了游戏照常能玩**。

---

## 0.0 实施结果（v1.3.0，2026-09-16）

**本文档第 1~9 节是当时的调研与设计；下面这段是实现后的事实，两者冲突时以本节为准。**

| 项 | 计划 | 实际落地 |
|---|---|---|
| 通道 | 服务端 `@cloudbase/node-sdk` | ✅ 服务端统一接入，但传输改成**官方 HTTP 通道**（见下） |
| 依赖 | `@cloudbase/node-sdk`（`optionalDependencies`） | ❌ **去掉了**。改为**零依赖原生 `fetch`** |
| 接入点 | 2 函数 + 3 调用点 | ✅ 与计划一致，`aiChooseDiscard` / `aiClaim` 一个字未改 |
| 难度档 | 快 / 强双模型 | ⛔ **没做成双模型**：本环境在控制台只启用了 `hy3` / `hy3-preview`（同族，实测答案一致），换模型名拉不开差距 → 改成「**引擎算牌 + 模型定夺**」单档策略 |
| 默认行为 | 关闭，需手动开 | ✅ 改成**默认启用**（用户要求）；没配环境变量 / 服务端不可达 / 用户关掉 都会自动回退本地逻辑，行为与 v1.2.53 逐字节一致 |
| 测试 | — | ✅ `tests/m59_ai_brain.js` **92 项**（候选集、预算解耦、HTTP 传输层桩测试）+ `tests/m60_ai_guard.js` **24 项**（防滥用闸门）+ `tests/m61_ai_settings.js` **45 项**（设置面板顺序与显隐） |

**实测环境事实（`askbuddy-d2gv2tz9xea0ff925`，2026-09-16 验过）：**

| 事实 | 值 |
|---|---|
| 本环境**已启用**的模型 | `hy3`、`hy3-preview`（同族；`deepseek-*` / `glm-*` / `qwen*` / `hunyuan-*` 均返回 `AI_MODEL_NOT_SUPPORTED`） |
| 一次决策往返耗时 | `hy3` **1.1~1.6s**（145~645 prompt tokens，输出 ~20 tokens） |
| 网关限流 | **60 请求/分钟**（超了返回 429 文案 `model RPM limit 60`）。⚠️ 这条**真的会撞上**，见 v1.3.5：改为在 `aibrain` 侧**主动配额节流**（滑动窗口 `AI_RPM_MAX`，默认 50/分），先排队再回退 |
| 单局成本 | 3 机器人 × 约 90 次决策 ≈ **19 资源点 ≈ ¥0.019/局** |
| 缓存 | 服务端侧同一局面秒回（本地结果缓存命中）；provider 侧 `cached_tokens` 本环境未观察到命中 |

**为什么不用 SDK，改走 HTTP：**

```
POST https://{envId}.api.tcloudbasegateway.com/v1/ai/{provider}/chat/completions
Authorization: Bearer <CloudBase API Key>
Content-Type: application/json
{ "model": "hy3", "messages": [...], "max_tokens": 80 }
→ OpenAI 形态：choices[0].message.content
```

- SDK 打的**就是同一个网关、同一套鉴权**，协议本身就是 OpenAI Chat Completions —— 换成原生 `fetch` 没有损失；
- 收益：**零依赖**（Render / WorkBuddy 的 `npm install` 不用再拉一棵 SDK 依赖树，构建更快更稳）；
- 收益：**可离线单测** —— `tests/m59_ai_brain.js` 起一个本地桩 HTTP 服务，就能验证请求形态、鉴权自动重试、错误分类，不花钱、不需要真凭据。
- 规范来源：`https://docs.cloudbase.net/openapi/ai_model.v1.openapi.yaml`（`/v1/ai/{provider}/{path}`，`provider=cloudbase`）。

**为什么最终只有一档（「引擎算牌、模型定夺」）：**

原计划「快档 = 模型自由选牌 / 强档 = 候选内定夺」用两个模型拉开难度差，但实测本环境
**只启用了 `hy3` / `hy3-preview`**（同族，三个局面答案完全一致），换模型名毫无差别。
于是难度差改为**策略差**，只保留更强的那一档：

| | 唯一一档 |
|---|---|
| 模型看到的牌 | 引擎算好的 **top-4 候选**，每张附 `shanten`（打掉后向听数）/ `danger`（放炮危险度） |
| 模型的任务 | 只在候选里**定夺** |
| 思考预算 | **3600ms**（与「出牌速度」档**无关** —— 那个档只管本地逻辑的节奏，走大模型的那台在设置里不显示它） |
| 设计意图 | **算牌**交给不会算错的确定性引擎，**取舍**交给模型（读牌河、判谁在做牌） |
| 实测输出 | `{"discard":"白板","reason":"同向听危险更低选白板"}`（确实按候选表取舍） |

模型若越出候选 → 判非法、回退本地；本地最优答案始终保留在允许集内，不会被候选表挤掉。
UI 上一个档位都不解释：**每个机器人**一行「用AI / 不用AI」（v1.3.1），只有不用 AI 的那台才出现「出牌速度」。

**环境变量（配在部署平台，绝不进仓库）：**

| 变量 | 说明 |
|---|---|
| `CLOUDBASE_ENV` | 环境 ID，必填 |
| `CLOUDBASE_API_KEY` | **API Key，必填**。是控制台「环境 → 访问管理 → API Key」里那串 **~916 字符的 JWT**，不是短串 |
| `AI_MODEL_FAST` / `AI_MODEL_STRONG` | 快档 / 强档模型，默认都是 `hy3`。⚠️ 必须是该环境**已启用**的模型（未启用 → `AI_MODEL_NOT_SUPPORTED`）；想真·双模型就在控制台启用一个更快的模型后改这里，**不用改代码** |
| `AI_PROVIDER` | 模型分组，默认 `cloudbase` |
| `AI_BOT_TIER` | 联机房间机器人托管档位：`off` / `fast` / `strong`（**默认 `strong`**，与单机「默认启用」一致；设 `off` 退回纯本地逻辑、不消耗额度） |
| `AI_AUTH` | `auto`（默认，先 Bearer 再裸 key）/ `bearer` / `raw` |
| `AI_BOT` | `off` = 整体停用 |

**自检命令**（不需要进游戏就能确认通没通）：

```bash
curl -s "$BASE/api/ai/status?probe=1" | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>console.log(s))"
# enabled:true + probe.ok:true  →  一切正常
# probe.ok:false              →  probe.detail 里就是失败原因（已翻译成人话）
```

> ⚠️ **实测踩坑：凭据报错要看错误码，别猜。**
> - `INVALID_CREDENTIALS` / `ACCESS_TOKEN_KID_INVALID`：JWT 结构可能完全正确（长度、`aud`、`role` 都对）但**网关查不到这个 `kid`** → 密钥已失效/被重新生成。此时换区域域名、换 `Authorization` 写法、换其它通道（如 PG 的 `exec-pgsql`）**都会报同一个错**，可以直接判定是密钥问题，不必反复试。
> - `AI_MODEL_NOT_SUPPORTED`：密钥没问题，是该模型没在控制台启用。
> - `EXCEED_TOKEN_QUOTA_LIMIT`：密钥没问题，是资源包额度用完。

**防滥用闸门（上线前必须有）**

`/api/ai/decide` 与 `/api/ai/status?probe=1` 是**公开无鉴权**的（单机模式在浏览器里同源调用），
而每次调用都花真实额度 —— 不设闸，等于把钱包挂在公网上。四道闸（`server.js` 顶部 `AI_RATE`）：

| 闸 | 规则 | 挡什么 |
|---|---|---|
| 同源校验 | 带 `Origin` 就必须与 `Host` 一致 | 拿别人的网页当代理、用玩家浏览器刷你的额度 |
| 单 IP 限流 | 240 次/分 | 单个刷子（正常牌局每分钟只有几十次决策） |
| 全局限流 | 300 次/分（进程级总闸） | **`X-Forwarded-For` 可伪造**，只靠单 IP 闸会被绕过；总闸兜底 |
| probe 冷却 | 同一 IP 20 秒一次 | `?probe=1` 会真打一次模型，最容易被刷 |

被挡时统一 `429 {ok:false,error:'origin'|'rate'|'global'|'probe_cd'}`，前端照常回退本地逻辑，**玩法零影响**。
另有网关自身的 60 次/分限制作为最后一道保险 —— 但它**不是保险、是硬墙**（v1.3.5 实测撞到过）：
撞上 429 会被算作失败，连败 3 次即开 30 秒熔断，玩家看到的就是「AI 掉线」。因此在 `aibrain` 里再压一道
**主动配额节流**（滑动窗口 `AI_RPM_MAX`，默认 50/分）：超了立即回退本地、不计失败、不动熔断，配额一松自动用回来。
回归保护见 `tests/m60_ai_guard.js`（24 项）与 `tests/m64_ai_rpm.js`（21 项）。

---

## 0.1 实施结果（v1.3.1，2026-09-16）：机器人按座位分别设置

**需求**：单机三台机器人要能分别设「用不用 AI」；联机按剩余机器人数量设置；设置时必须知道是哪一家。

| 项 | 落地方式 |
|---|---|
| 单机 | 每台一行「用AI / 不用AI」，选「不用AI」出现这一台的「出牌速度 慢 / 中 / 快」。存 `sjm_ai`（`{seats:[…], spd:[…]}`，兼容旧 `{on}` / `{tier}` 存档） |
| 联机 | 机器人 = **空位**。房主在等待页按空位逐台设置，写进房间规则 `cfg.aiSeats` / `cfg.spd`（**长度 4 的数组，下标 = 真实座位号**），随「确定」提交给服务端；客户端自设无效（机器人跑在服务端） |
| 认位 | 界面按「下家 / 对家 / 上家 + 座位号」标注（单机再加机器人名，如 `机器人1 · 阿芳`／`下家（你右边，座位 1）`）；**联机不写「机器人 N」**，直接 `机器人 · 下家`／`座位 3 · 空位` —— 空位可能是座位 1 和 3 这种不连续的，编号会和座位号对不上 |
| 引擎 | 新增 `aiSeatOn(i)`（默认 true，老存档/老规则行为不变）与 `spdOf(p)`（按座位取节奏，脏值退回 `CFG.speed`）；`decideDiscard / decideClaim` 在「该座位不用 AI」时**直接返回本地答案、不发请求**；`waitBot` 与 5 处 sleep 改成按座位取 |
| 注入 | 只要有一个座位用 AI 就注入大脑（`applyAiTier`）；三台全关 → 完全不注入，零网络开销 |
| AI 状态 | 修「看不到毫秒」：状态查询返回可用后**自动补一次** `?probe=1` 实测（每页一次），结果写进状态行；另加「测试连接」按钮随时重测。实测失败如实写原因，不空着 |
| 取消 | 全局「机器人速度」行（`speedRows` / `setCfg6`）删除；`CFG.speed` 作为兜底值保留（真人节奏仍按它走） |
| 回归 | `tests/m61_ai_settings.js` 重写 **54 项** + 新增 `tests/m62_ai_seats.js` **35 项** |

---

## 0.4 实施结果（v1.3.4，2026-09-17）：提示词补齐 4 条规则 + 听口剩余张数进入决策

- **规则知识补齐**（`RULES_BASE`，system 段仍是**逐字常量**，继续命中模型侧上下文缓存）：
  七小对随 `view.sevenPairs`、清混碰锁门 `view.lock`、无花果只能自摸 `view.wuGuoHua`、各档番值；
  user 段新增 `cur`（这手牌当前的底 / 番）。强档候选说明补 `left` —— 打完若听牌，听口在台面上的剩余张数。
- **听口剩余张数**：引擎侧新增 `leftFor` / `aliveWaits` / `aliveLeft`。`leftFor(t, p)` 是原 `visibleLeft(t)` 的泛化
  （按座位视角，人类与 AI 同源同口径）；`aiRankDiscards` 候选带 `left`，`left === 0`（听死）按「没听」退回一向听。
- **代价**：system 段加长约 300 token，**首次调用会 miss 一次上下文缓存**，其后照旧命中。
- **回归**：m63（40）+ m59（92）+ m61（80）+ m62（34）全过；真凭据 probe 正常（1869ms）。

## 0.3 实施结果（v1.3.3，2026-09-16）：修「实测毫秒不自动出现」

| 项 | 落地方式 |
|---|---|
| 根因 | `refreshSet()` 用 `#sheet.classList.contains('on')` 判断「设置面板开着」，但弹窗显隐挂在 `#mask` 的 `.hide` 上 → 判断恒假 → 探测结果回来也不重绘（切开关时 `setAiSeat` 调到 `openSet()` 才「看见」） |
| 面板识别 | 新增 `sheetOpen()`（读 `#mask` 的 `.hide`）；`openSheet(html, kind)` 加弹窗种类标记，设置面板传 `'set'`；`refreshSet()` 需「开着 && 是设置面板」两条件 —— 同时避免把规则 / 记录弹窗顶掉 |
| 打开即测 | `openSet()` → `maybeAutoTest()`：未拿到成功结论、不在探测中、距上次尝试 > 5 秒才发；非 http(s)（本地文件）直接跳过 |
| 冷却取用缓存 | 服务端 `/api/ai/status?probe=1` 撞 `probe_cd` 时改为返回 `aibrain.probe(false)` 的 60 秒缓存结论（`cached:true`），不再回「被限流」；客户端文案加「（沿用刚测的结论）」 |
| 定时看门狗 | `armProbeWatchdog()`：`setTimeout` 到 `PROBE_STUCK_MS` 若仍在探测 → 复位 + 记超时 + `refreshSet()`；探测正常结束时清掉 |
| 实测耗时 | 本机真凭据：打开设置后 **约 1.5 秒**面板出现 `✅ 实测调用成功 1426ms`（此前撞冷却要等 22 秒） |
| 回归 | `tests/m61_ai_settings.js` **80 项**（新增第 9 / 10 节）；真实端到端 14/14 |

## 0.2 实施结果（v1.3.2，2026-09-16）：去掉速度选择 + 修「AI 测试结果不显示」

| 项 | 落地方式 |
|---|---|
| 本地机器人节奏 | 固定「快」：`BOT_SPEED = 1.8`；`CFG.speed` 与默认 `CFG.spd` 全为 1.8；单机存档不再写 `spd`（老值忽略），面板上以一行只读说明取代速度选择器 |
| 联机 | `cfg.spd` 键保留（`defaultCfg` 默认全 1.8），服务端 `CFG_ALLOW.spd` 仍接受 0.6 / 1 / 1.8 —— 老客户端、老房间规则不受影响；引擎 `spdOf(p)` 依旧按座位读 |
| 探测超时 | 请求带 `AbortController`，`PROBE_TIMEOUT_MS = 20000`；超时文案「服务端响应超时（20 秒），机器人先按本地逻辑出牌」 |
| 复位兜底 | `AI_SRV.probing` 的复位移到 promise 链末端，成功 / 失败 / 抛错三条路径都会执行（此前任何一步抛错都会让界面永久停在「测试中」） |
| 看门狗 | `probeBusy()`：`probeAt` 距今超过 `PROBE_STUCK_MS`（30 秒）自动判超时并放行；面板渲染时调用，卡死状态会被下一次重绘清掉 |
| 自动重测 | `scheduleProbeRetry()`：只要没拿到成功结论就自动重试，上限 `AI_RETRY_N = 3`；冷却挡回等 `PROBE_CD_MS`（22 秒）、其它失败等 5 秒；手动点「测试连接」重置预算 |
| 文案 | 重测期间带出上次耗时（`已连接 hy3　⏳ 正在实测调用…（上次 1338ms）`）；冷却期写「冷却结束自动重测」；不可达 / 超时与「未配置大模型」分开表述 |
| 回归 | `tests/m61_ai_settings.js` **66 项**（新增看门狗 / 冷却自动重测 / 连点只发一个请求等 12 项）+ `tests/m62_ai_seats.js` **34 项** |

---

## 0. 结论速览

| 问题 | 答案 |
|---|---|
| 接入点有几个？ | **只有 2 个函数**：`aiChooseDiscard`（出牌）、`aiClaim`（碰/吃/杠/胡）；调用点 **3 处** |
| 单机、联机都要改吗？ | 不用分别改。两个模式共用同一份引擎，改一次两边都生效 |
| 用哪条通道？ | **推荐服务端统一接入**（我们自己的 `server.js` + `@cloudbase/node-sdk`）；浏览器直连作为备选 |
| 成本？ | 约 **2~5 分钱一局**（3 个机器人打满一局）；个人版套餐 ¥19.9/月 ≈ 800~2000 局 |
| 最大风险？ | ① AI 作弊（提示词里泄露他人手牌）② 延迟（每回合多 1~3 秒）③ 模型返回非法动作 —— 三个都有确定解法，见第 5 节 |
| 建议第一步 | 做 P0（抽钩子 + 降级框架，行为零变化，可跑单测验收），拿到 env 后再打通 P1 |

---

## 1. 现状盘点：改造面到底有多大

机器人决策目前全部集中在引擎的 4 个函数里，**都是纯同步函数**：

| 函数 | 位置 | 作用 | 调用点 |
|---|---|---|---|
| `aiTargetOf(p)` | index.html:1570 | 定做牌方向（碰碰胡/混一色/清一色/风一色） | 被下面两个内部调用 |
| `aiPlan(p)` / `tileFitsPlan` | 1629 / 1636 | 把方向翻译成「只吃碰哪些花色」 | 同上 |
| `aiChooseDiscard(p, ban)` | 1653 | **出哪张牌**（向听 + 有效牌 + 危险度 + 中张价值打分） | 2888（敲定后摸打兜底）、2894（机器人回合） |
| `aiClaim(p, tile, from, opts, isNext)` | 1684 | **碰/吃/杠/胡要不要** | 3056（`pickClaim` 内） |

谁在用它们：

- **单机**：引擎跑在浏览器里，3 个 `isBot` 座位走 `aiChooseDiscard` / `aiClaim`；
- **联机**：引擎跑在 `server.js`（无头），**空座位自动变机器人**（server.js:318 `G.players[i].isBot = true`），掉线托管也走同一路径；
- **测试/仿真**：`proto/server/simulate.js` 四家全 AI 连打多局做一致性验证；`tests/m36_m37_m39` 等**直接同步调用** `aiClaim/aiChooseDiscard` 并断言结果。

> ⚠️ 这条决定了架构：**不能把这两个函数改成 async**（会连带打断 simulate 与十几个单测的确定性）。
> 正确做法是**保留同步版本当"本地大脑"，另加一层 async 包装 `decideDiscard/decideClaim`**：先同步算出本地答案当兜底 → 并发去问大模型 → 在「思考预算」内谁赢用谁 → 校验合法性。这样仿真/单测完全不受影响，线上行为却可以是大模型。

---

## 2. CloudBase 侧的 AI 能力（调研结果）

### 2.1 三条官方接入通道

| 通道 | SDK / 入口 | 认证方式 | 适合 | 本项目评估 |
|---|---|---|---|---|
| **A. 浏览器直连** | `@cloudbase/js-sdk` v2<br>`app.ai().createModel('cloudbase')` | Publishable accessKey（可公开）+ 匿名登录 | 纯前端应用 | 可行，但要给 index.html 引入外部 ESM 依赖 + 配「Web 安全域名」；且 `file://` 打开时 origin 为 null 无法白名单 |
| **B. 服务端直连** ★推荐 | `@cloudbase/node-sdk` **≥3.16.0**<br>`tcb.init({env}).ai()` | 云函数内免密钥；自建服务用 `secretId/secretKey` 或 API Key | 有后端/常驻服务的应用 | **本项目最顺**：`server.js` 已是常驻 Node 服务，一份密钥、一处限流、一处日志 |
| **C. 云函数 + HTTP 访问服务** | 自己写个云函数暴露公网 URL | 密钥全在云上 | 无可信后端的纯静态站 | 备选：好处是 `file://` 也能用，代价是加一层部署 + CORS 配置 |

### 2.2 调用形态（两端 API 一致）

```js
// 服务端（@cloudbase/node-sdk >= 3.16.0）
const tcb = require('@cloudbase/node-sdk');
const app = tcb.init({ env: process.env.CLOUDBASE_ENV });
const model = app.ai().createModel('cloudbase');           // GroupName 只有三种合法写法
const res = await model.generateText({
  model: 'deepseek-v4-flash',                              // 具体模型 id 写在这里
  messages: [{ role: 'user', content: '...' }],
  temperature: 0.6,
  max_tokens: 64,
});
console.log(res.text, res.usage);
```

- `createModel()` 的合法参数**只有三种**：`'cloudbase'`（主托管模型组）、`'hunyuan-exp'`（如环境仍返回该内置组）、`'custom-*'`（自定义接入）。把 `'deepseek'`、具体模型 id 传进去都是错的。
- 具体模型（`deepseek-v4-flash` / `hy3` / `glm-5.2` …）写在 `generateText` 的 `model` 字段。
- 支持 `streamText()`（流式）与 `registerFunctionTool()`（工具调用）；我们只需要 `generateText` + JSON 输出。

### 2.3 上线前必须在控制台做的四件事

1. **开通/确认环境**（有 env id）；
2. **AI → 生文模型**：查当前可用模型（`DescribeAIModels`），**并把要用的模型显式启用**（`UpdateAIModel`）——官方明确「默认不启用任何模型」；
3. **计费切到「资源点」**（2026-06-17 起内置模型走资源点计费）；
4. 若走 A 通道：配置 **Web 安全域名**（线上域名 + localhost:8080）+ 允许**匿名登录** + 生成 **Publishable Key**。

### 2.4 计费（实测口径）

- 单位：**资源点**，`1000 点 = ¥1`，与云资源同一池子。
- 文本模型按 token 计：以 DeepSeek-V4-Flash 直供为例 —— **未命中上下文缓存 1000 点/百万 tokens**，**命中缓存 20 点/百万 tokens**（差 50 倍！）。
- 免费额度：每个计费周期赠送 10 万通用 Token；免费体验环境 3000 点/月。
- 套餐参考：个人版 ¥19.9/月 = 40,000 资源点。

### 2.5 已知限制

- **并发与额度**：可能返回 429（`EXCEED_TOKEN_QUOTA_LIMIT` 或并发受限，与套餐档位相关）→ 必须做串行队列 + 失败降级；
- 单次调用超时：SDK 默认 15s，可传 `timeout` 放大到 60s——**我们反而要往下压**（见 5.3）；
- 模型输出不保证是合法 JSON，需要容错解析。

---

## 3. 方案对比与推荐

| 维度 | A 浏览器直连 | **B 服务端代理** ★ | C 云函数 HTTP |
|---|---|---|---|
| 改动量 | 中（前端引 SDK、改引擎注入） | **小**（后端加 1 个模块 + 1 个路由） | 中（多一个云函数 + CORS） |
| 密钥安全 | Publishable Key 公开可控 | **密钥只在服务端环境变量** | 密钥在云上，最好 |
| 单机（线上打开） | ✅ | ✅ | ✅ |
| 单机（`file://` 本地打开） | ⚠️ origin=null 需额外处理 | ❌ 自动降级本地 AI | ✅ |
| 联机（server.js 权威） | ❌ 客户端无权替他人决策 | ✅ | ✅ |
| 前端依赖 | 破坏「单文件自包含」 | **不破坏** | 不破坏 |
| 限流/缓存/日志/成本控制 | 分散在客户端 | **集中一处** | 集中在云函数 |
| 调试便利 | 一般 | **最好（服务端日志 + 可离线 mock）** | 一般 |

**推荐方案 B**：`server.js` 加一个 `POST /api/ai/decide`（与 index.html 同源，无 CORS 问题），单机浏览器调它，联机在服务端进程内直接调同一模块。密钥只放环境变量，前端不引任何 SDK，游戏继续是「单文件 + 一个 Node 服务」的形态。

> 官方文档建议「网页 AI 不要再包一层后端代理」——那条建议针对的是**没有后端的纯前端聊天应用**（那时直连更简单）。我们这里已经有权威服务端，而且需要**密钥保护 + 动作合法性校验 + 成本限流**，服务端接入是更合适的形态。

---

## 4. 目标架构

- 新增 `proto/server/aibrain.js`：唯一的大模型出入口
  - `buildPrompt(view)`：把「该机器人的可见信息」压成紧凑提示词
  - `decide({ kind, view, options, budgetMs })`：调 CloudBase → 解析 → 白名单校验 → 返回 `{tile|action, reason}`
  - 内置：**串行队列**（避免 429）、**结果缓存**（同局面哈希命中直接复用）、**超时**、**错误计数与自动熔断**
- `server.js` 新增 `POST /api/ai/decide`：解析 body → 调 `aibrain.decide` → 返回 JSON；未配置 env 时直接返回 501，客户端静默降级
- 引擎侧（index.html，切分点之前 → 自动被 extract 进 `engine.js`）：
  - `AI_BRAIN` 注入点 + `aiView(p)`（构造「该 AI 视角」的视图，**纯函数**）
  - `decideDiscard(p, bans)` / `decideClaim(p, tile, from, opts, isNext)` 两个 async 包装
  - 3 处调用点改为 `await`（`discardTurn` 与 `pickClaim` 本来就是 async，改动极小）
- 三层降级阶梯：**大模型（预算内）→ 本地启发式（永远先算好）→ 最保守安全牌（打摸到的那张）**

数据流（单机 / 联机）：

```
单机：浏览器引擎 ──HTTP /api/ai/decide──▶ server.js ──node-sdk──▶ CloudBase AI
                    （同源，无 CORS）         │
                                            ├ 未配置/超时/报错 → 本地启发式兜底
联机：server.js 引擎（房间权威）──────────直接调用 aibrain.decide()（不过 HTTP）
```

---

## 5. 关键设计细节（能不能落地的分水岭）

### 5.1 绝不作弊：只喂「该 AI 自己能看到的信息」

| 可以给 | 绝不能给 |
|---|---|
| 它的手牌、它的副露/花、它的听牌 | **其他三家的手牌** |
| 全场已打出的牌（牌河）、公开副露与杠 | **牌墙剩余顺序 / 下一张是什么** |
| 自己的座次、庄家、局数、剩余牌数、已现张数 | 其他家是否听牌的内部标记（可给"公开推断"，不给真值） |
| 规则摘要、当前玩法（敲麻/清混碰）、禁打限制 | 其他玩家的敲定状态真值（同理只给公开可见的） |

实现上：`aiView(p)` 只读 `p.hand / p.melds / p.flowers` + `G.players[*].discards / melds`（公开）+ `wallLeft()`（剩余张数），**不读** `q.hand`。联机侧本来就有 `projectFor` 的投影纪律，单机侧必须靠这个纯函数守规矩，上代码时要单独 review 一次。

### 5.2 绝不打出非法牌：白名单 + 二次校验

- **输入侧**：只把「本次合法可选项」列进提示词。出牌给 `bans` 过滤后的手牌列表；碰吃给 `claimOptions` 的结果，带稳定编号。
- **输出侧**：要求模型只回 JSON，例如
  - 出牌：`{"discard":"5筒","reason":"留搭子做混一色"}`
  - 动作：`{"action":"碰","reason":"..."}`（可选值：`胡/杠/碰/吃/过`）
- **校验侧**：程序把牌名映射回编码（**不要让模型输出数字编码**，模型极易搞错编码表），然后
  1. 是否在合法集合内？不在 → 走本地答案；
  2. 出牌是否命中 `bans`（吃碰禁打）？命中 → 走本地答案；
  3. 手牌里是否真有这张？没有 → 走本地答案。
- 这一层校验在引擎侧（服务端权威），即使模型被"越狱"也不会破坏规则。

### 5.3 延迟：把 1~3 秒藏起来

| 手段 | 说明 |
|---|---|
| **思考预算** | 默认 1200ms；超时立即用本地启发式（本地答案在发请求前就已算好，**零等待兜底**） |
| **与速度档联动** | 设置里已有 `CFG.speed`；"快/中/慢"分别对应 600/1200/2200ms 预算 |
| **系统提示词固定前置** | 规则与输出契约放最前面且逐字不变 → 命中上下文缓存，**同款模型延迟和价格都降** |
| **紧凑状态** | 一手牌 + 场面压到 150~250 tokens 输入、输出 ≤ 40 tokens（`max_tokens: 64`） |
| **预设兜底** | 网络/额度异常时直接切本地逻辑。⚠️ 设置面板显示「AI 已降级」**尚未落地**：`aiBrainStat()`（成功/回退计数 + 最近原因）已实现但无人调用，目前只有 `/api/ai/status` 的 `failStreak` / `lastError` / `rpm` 可查 |

> 体感预估：单机一回合 = 3 家 × (0.5~1.2s) ≈ 2~4 秒（现在是 1.5~2 秒）。可接受；嫌慢就给机器人加「思考中…」气泡，反而更像真人。

### 5.4 成本控制（一局算给你看）

按「3 个机器人、每家约 30 次决策 = 90 次调用」、每次输入 450 tokens（其中系统提示 300 tokens 命中缓存）、输出 20 tokens 估算：

| 项目 | 用量 | 点数 |
|---|---|---|
| 输入·命中缓存（系统提示） | 27k tokens | 0.54 点 |
| 输入·未命中（局面状态） | 13.5k tokens | 13.5 点 |
| 输出 | 1.8k tokens | ~5 点（按输出单价高于输入估） |
| **合计** | | **≈ 19 点 ≈ ¥0.019/局** |

- 一局约 **2 分钱**，100 局约 ¥2；即使完全不吃缓存也就 ¥0.05/局。
- 个人版 40,000 点/月 ≈ **2000 局**；免费体验环境 3000 点/月 ≈ 150 局。
- 三重省钱手段：**上下文缓存**（50 倍价差）、**同局面结果缓存**、**可选"仅关键决策走大模型"**（只在可胡/可碰吃/听牌巡目调用，其余走本地，成本再降 60%+）。

### 5.5 顺带白拿的体验升级

- **机器人思考气泡**：模型顺手回一句 `reason`（如"留搭子做混一色，7万先过"）→ 牌桌上显示，玩家能直观感到"这 AI 是真的在打牌"。这是最容易做出惊艳感的一步。
- **难度分级**：同一份代码，用不同模型/温度档位当"简单/普通/困难"（如 `hy3` 快手感 vs `deepseek-v4-flash` 更稳）。
- **复盘**：把 `reason` 存进牌局日志，打完能看"AI 为什么这么打"。

### 5.6 开关与灰度（一定要有）

- 设置面板新增机器人分组：**每台机器人各自「用AI / 不用AI」，默认都用**；只有不用 AI 的那台才显示「出牌速度」（v1.3.1）；
- 环境变量 `AI_BOT=on/off` 可在服务端整体关停；
- 未配置 `CLOUDBASE_ENV` 时前端自动隐藏该选项，接口返回 501 → 静默降级。

---

## 6. 落地步骤（每步可独立验收）

| 阶段 | 内容 | 不改行为的保证 / 验收 |
|---|---|---|
| **P0** | 引擎侧抽 `AI_BRAIN` 注入点 + `aiView(p)` + `decideDiscard/decideClaim` 包装（`AI_BRAIN` 为空时 100% 走本地逻辑）；3 处调用点改 `await` | `extract.js` 90/90 补丁命中；全量 31 个 JS 测试与 `simulate.js` 结果逐位一致（parity）；单机打 3 局手感不变 |
| **P1** | `proto/server/aibrain.js` + `server.js` 的 `/api/ai/decide` + 客户端在**单机**注入 HTTP 大脑 + 设置开关 | 服务端日志能看到 `prompt/usage/latency`；关掉开关行为回到 P0；断网时自动降级 |
| **P2** | 联机房间内直接调用同一模块（空位/掉线托管可选用大模型，给房主开关） | 联机冒烟：4 人房 + 1 机器人 / 掉线托管；确认不会让真人等到超时 |
| **P3** | 思考气泡、难度档、缓存与熔断调优、成本面板 | 观察一周 token 用量，调整预算与模型档位 |

---

## 7. 需要你提供 / 确认的清单

**必需（服务端方案 B）**
1. CloudBase **环境 ID**（env id）；
2. **选哪个模型**：建议先启用 `deepseek-v4-flash`（快、便宜、够用）或 `hy3`；标准版以上还能选 `glm-5.2` 这类更强模型；
3. **服务端密钥**：`secretId/secretKey`（控制台→访问管理）**或** API Key —— 只会写进环境变量（Render/WorkBuddy 的项目环境变量），**绝不进仓库**；
4. 计费方式：是否已切「资源点」，有没有 Token 包 / 免费额度。

**只有走方案 A（浏览器直连）才需要**
5. Publishable Key；6. Web 安全域名（`shanghai-mahjong-3bxg.onrender.com`、`yidaolai-mahjong-*.app.workbuddy.host`、`localhost:8080`）；7. 是否允许匿名登录。

**只有走方案 C（云函数）才需要**
8. 允许我提交一个云函数 + 开启「HTTP 访问服务」。

---

## 8. 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| 模型输出非法动作 / 乱码 | 打错牌、破坏规则 | 白名单 + 二次校验 + 一律回退本地答案（5.2） |
| 提示词泄露他人手牌 | AI 作弊，游戏失去意义 | `aiView(p)` 只读自己的手牌，代码 review 单列一条（5.1） |
| 延迟拖慢节奏 | 手感变差 | 思考预算 + 零等待兜底 + 速度档联动（5.3） |
| 429 / 额度耗尽 / 网络抖动 | 机器人卡住 | 串行队列 + 熔断 + 降级本地，前端显示"已降级"（2.5 / 5.3） |
| 成本失控 | 账单超预期 | 默认关闭 + 缓存 + 关键决策模式 + 用量日志（5.4 / 5.6） |
| `file://` 本地打开无后端 | 离线时用不了大模型 | 自动降级本地逻辑（方案 B 的已知代价；介意就走方案 C） |
| 联机时托管掉线玩家 | 全桌都在等模型 | 联机默认仍用本地托管，大模型仅用于主动选的 AI，或给房主开关 |

---

## 9. 我的建议

先做 **P0 + P1**（一个改动批次，本地就能验收：开开关走大模型、关掉完全回到现在的手感），同时你把 **env + 模型 + 密钥** 给我，我接通后跑一次实测，把真实延迟与 token 用量贴给你，再决定要不要做 P2/P3。

如果你想先看到效果再决定，也可以只做 **P1 的最小版**：单机模式 + 1 个机器人走大模型，其余保持本地逻辑。
