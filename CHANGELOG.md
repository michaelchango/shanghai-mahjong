# 更新日志

## v1.2.19（2026-09-03）

### 规则补充：清混碰「吃过即锁门」（从判断到按钮全链路）

清混碰没有平胡兜底，**一旦某家吃过顺子（chow 副露），本局胡牌方向就被锁死为该顺子的花色**（只能做这门混一色 / 清一色）——之后其他花色的吃 / 碰 / 杠都会让牌型必然不合法，逻辑上不该再提示。修复从选项生成源头统一拦截，真人按钮、AI 决策、服务端判定全部一致：

1. **锁门判定** `lockedSuitOf(p)`：玩家副露里有吃牌即返回锁定花色（0/1/2）；多门吃牌返回 -1（防御）；没吃过返回 null（碰碰胡 / 方向未定仍开放）。**敲麻玩法有平胡兜底，不锁门**（`lajiHu` 开时不启用）。
2. **`claimOptions` 拦截**：锁门后，别人打出的牌若既非本门数牌、也非字牌（风 27-30 / 中发白 31-33），吃 / 碰 / 杠选项一律不再生成——**按钮不会出现，AI 也收不到这些选项**；本门与字牌正常保留（混一色含字牌），胡牌(hu)不受影响。
3. **`selfKongOptions` 拦截**：吃过锁门后摸到其他花色的 4 张，不再提示暗杠 / 加杠（同理会锁死牌型）。
4. 真人吃碰按钮、机器人花色锁、服务端询问三者共用 `claimOptions`——同一把锁，全链路生效。

### 测试

- 新增 `tests/m41_suit_lock.js`（14 项）：清混碰吃过萬后——打筒不提示碰/杠、打萬/風/中發白正常提示、跨门顺子不给吃、本门顺子可吃、摸齐筒 4 张不提示暗杠、本门/字牌暗杠加杠仍提示；敲麻（lajiHu 开）吃过萬后打筒仍可碰、暗杠照常（不锁门）；只碰没吃不锁门。
- 新增 `tests/m42_lock_ui.py`（6 项端到端）：单机清混碰注入「已吃萬」，真人按钮区打筒（其他花色）无「碰/杠/吃」按钮、打風（字牌）有「碰」按钮。
- 回归：Node 14 脚本全绿（unit 62 / m16 18 / m17 10 / m19 28 / m23 20 / m25 13 / m30 7 / m33 21 / m36 42 / m37 19 / m39 27 / m41 14）；m13 / m38 / m40 / m42 Playwright 全过、0 JS 错误；extract 8/8（72/72），显式退出码 0；版本号同步 v1.2.19。

## v1.2.18（2026-09-03）

### 新增 / 调整（4 项，本轮反馈）

1. **单机入口先弹设置**：点「单机游戏」不再直接开局，先弹设置面板，底部按钮「返回 / 开始游戏」——返回=回首页不开局、开始游戏=按当前设置开局（改完点选项即时反映在面板上，未点开始前不生效）。
   - 单机设置改为**草稿制**（与联机房主一致）：局内点右上角设置修改后，点「完成」会弹确认「局内修改游戏设置会清空本局记录并重新开局」；取消=不应用、本局继续，确定=应用并重开（新发牌、记录清空）。无改动点「完成」仅关闭。
2. **点牌算剩余张数：吃碰杠区也高亮 + 高亮加强**：选中手牌时，四家牌河与自己吃碰杠区（副露）里的同牌一起高亮——副露和牌河一样是「已见牌」，算台面剩几张不能漏。高亮样式从 2px 蓝框升级为 **3px 亮蓝框 + 双层光晕**，一眼能扫到；副露高亮随选中/落回实时刷新（渲染路径补了 `renderMe`）。
3. **唯一听法提示**：已敲定且只有一种听法（唯一听张，如单钓）时，轮到出牌在按钮上方显示「听牌中 → xx · 还有 n 张」（用台面可见数算剩余）；多种听法时底部 tingLine 已全列，不打扰。
4. **机器人更智能：边打边判断、不无脑碰**（清混碰玩法显著改观）：
   - **目标实时刷新**（核心）：旧版开局算一次 target 就整局不换——`aiClaim` 里手动传的目标会实时被重算、每次摸牌出牌前也重估。碰出同门副露后该转混/清一色会立刻转，不会再一条道走到黑。
   - **碰牌做「最优弃牌」模拟**：旧版只算「碰完两张」的向听，孤对一碰向听-2 看着大赚、实则被迫拆好搭子（无脑碰的来源）。新版模拟「碰 + 弃一张最优」后的真实向听，已听牌（0 向听）时不再碰——守护听牌。pung 加成 0.6→0.3。
   - **向听量纲再修正**：混/清一色废牌权重 1/2→1/3（半价仍把散牌一色算到封顶 8，对碰碰胡毫无竞争力）；平手（同向听）倾斜混/清一色（能吃顺子更易成型）；碰碰胡默认偏心 +0.2→+0.1。
   - **风一色不空想**：字牌 ≥6 张且向听真领先才押 4 番；本门不足 2 张不参与混/清竞争（修「全风牌被 mix* 抢目标」）；清一色只在与混一同向听时押（旧版差 1 步就冲 3 番太贪）。
   - 实测（24 局四家机器人）：清混碰下吃牌 8→24、碰 224→164（克制了），终局听牌 52→58/96；起手目标分布 pung ~94%→75%、混一色 ~19%、清一色 ~3%、风一色 ~2%；敲麻玩法回归不变。

### 测试

- 新增 `tests/m40_solo_setup.py`（17 项端到端）：单机入口「返回 / 开始游戏」、局内完成→「重新开局？」确认（取消不改/确定应用重开）、副露高亮与加强样式断言、唯一听张「听牌中 → 白 · 还有 n 张」。
- `tests/m39_zhongfabai.js` 第 6 节随 AI 实时目标更新（未听牌朝碰碰胡会碰「中」；已听牌不碰守护听牌）。
- 19 个 Playwright 脚本适配单机入口新流程（点单机后追加「开始游戏」）；m9 发现并修复「联机返回首页后 roomNo 残留导致单机入口误判成房间模式」（hBtnSolo 点击前清理联机残留）。
- 回归：Node 13 脚本全绿（unit 62 / m4 / m13 7 / m16 18 / m17 10 / m19 28 / m23 20 / m25 13 / m30 7 / m33 21 / m36 42 / m37 19 / m39 27）；Playwright 29 个脚本全过（m1~m40 全套）、0 JS 错误；extract 8/8（72/72），显式退出码 0；版本号同步 v1.2.18。

## v1.2.17（2026-09-03）

### 规则纠正（上轮需求写错）：清混碰里中发白不算花，算牌

v1.2.16 曾按「清混碰中发白当花」实现并固化测试——本轮纠正为：**敲麻**维持「中发白算花」（108~119 上墙，摸到即补）；**清混碰**下中发白**算牌不算花**（按 31 中 / 32 發 / 33 白 上墙，可碰、可组刻子/将牌，作字牌参与混一色 / 风一色）。首页副标「中发白算花」文案同步去掉。

### 实现要点

1. **双态上墙（buildWall）**：墙仍 144 张、玩法间总数不变——敲麻发 12 个分散编码（108~111 都渲染成「中」，防凑刻的既有设计）；清混碰发同码各 4 张（31/32/33），真正能组刻子。
2. **编码空间 31→34**：`PLAYCNT = 34`（敲麻下 31~33 不上墙，相关循环空转，零回归）。
3. **番型判定防误判**：`evaluateShape` 里中发白计入「字牌」——「萬一门 + 白板将」正确判**混一色**，而不是把白板漏掉后误判成**清一色**（本需求最易踩的坑，已加回归测试）。
4. **混一色 / 风一色口径**：混一色 = 一门数牌 + 字牌（风 + 中发白）；风一色 = 全字牌（风 + 中发白，七对形态天然可凑齐 7 种字）；清一色仍纯数牌、不含任何字。
5. **听牌 / 渲染 / AI**：`getWaitsHand` 听牌枚举扩到 0~33（听白板正常显示）；`faceKey`/`tileName` 补 31~33（牌面走既有的 chun/hatsu/haku SVG）；AI 会碰中发白、混一色目标可留字、清一色目标弃字。
6. **文案**：首页副标「中发白算花 · 敲麻/清混碰」→「敲麻 / 清混碰」；设置「玩法」行与规则页补玩法差异说明（敲麻算花 / 清混碰算牌）。

### 测试

- 新增 `tests/m39_zhongfabai.js`（26 项）：buildWall 双态（敲麻 12 编码花 / 清混碰 31~33 各 4 张牌、均 144 张）；编码工具（非花、字牌 suit=3、中/發/白、faceKey→chun/hatsu/haku）；番型判定（萬+白板=混一色非清一色、全字刻=风碰、全字七对=风一色、字刻入碰碰胡/混碰）；听牌含白板；可碰/杠不可吃；AI 态度。
- `tests/m36_qinghunpeng.js` 第 6 节与 `tests/m37_qhp_game.js` 第 3 节断言随需求纠正更新（敲麻形态仍是花 + 清混碰形态算牌 + PLAYCNT=34）。
- 浏览器冒烟：单机切清混碰注入含中发白手牌，13 张牌面渲染无 JS 错误、打出白板牌河记「白」。
- 回归：Node 13 脚本全绿（unit 62 / m4 / m13 7 / m16 18 / m17 10 / m19 28 / m23 20 / m25 13 / m30 7 / m33 21 / m36 42 / m37 19 / m39 26）；Playwright 27 个脚本全过、0 JS 错误；extract 8/8（72/72），显式退出码 0；版本号同步 v1.2.17。

## v1.2.16（2026-09-03）

### 新增 / 调整（4 项，本轮反馈）

1. **圆心无人决策时不显示**：旧版无人在决策时显示「——」占位，桌心看着多余。现在直接留空，有人决策时仍是纯数字倒计时。
2. **首页名称改为「一道来麻将」**：`<title>` 与首页标题同步（副标「中发白算花 · 敲麻 / 清混碰」），版本号 v1.2.16。
3. **设置「是否允许垃圾胡」改为两个选项「敲麻 / 清混碰」**：语义与原来完全一致——敲麻=允许垃圾胡（开）、清混碰=不允许垃圾胡（关），只是文案对玩家更直白；服务端 `CFG_ALLOW` 校验与存储格式不变，老房间规则无缝兼容。
4. **修复：清混碰下机器人不按规则打（乱吃多门）**。排查出三个真缺陷，全部修复：
   - **AI 目标太窄**：旧版清混碰下只在「碰碰胡 + 混一色」里选目标，机器人永远做不出清一色 / 风一色。修复：纳入清一色 / 风一色方向（清一色只比混一色多 1 步以内就押 3 番；风一色同向听押 4 番；但向听已封顶 8 =「做不成」的不抢，避免起手一门不占优时硬送）。
   - **吃碰无花色约束**：旧版机器人吃/碰/杠不看自己做牌方向，清混碰下还在吃另外两门。修复：新增 `aiPlan()`（风一色只风牌、混一色本门+风、清一色只本门、碰碰胡不吃顺子）+ `tileFitsPlan()` 花色锁，`aiClaim()` 对不符花色的吃/碰/杠一律不动；收益也改为按「目标牌型」的向听计算（旧版拿全类型最佳向听说事，明明在往混一色做、碰本门牌却因「对最佳向听无改善」而不动）。
   - **向听量纲错配（根因）**：`shantenColor()` 把非本门废牌按「每张 1 向听」全额累加，混一色能算出 10~14，远超向听理论上限 8（4 面子+1 将）；而碰碰胡向听完全不计废牌。两者不同量纲导致跨类型比较永远偏向碰碰胡——**清混碰玩法实际退化成只剩碰碰胡，谁也做不出混一色/清一色**。修复：废牌按半价计入（换牌的同时也在推进搭子，旧算法重复计数）并封顶 8。修复后随机起手目标分布：碰碰胡 ~94%（起手散牌它确实最近）、混/清一色 ~4.5%（本门占优才押）、风一色 ~1%（风牌够多才押），与麻将常识一致。
   - **确认项：清混碰时中发白当花** —— 属既有规则（中发白 108~119 为花牌不参与组牌、`PLAYCNT=31`），本轮补测试固化，无代码改动。

### 测试

- 新增 `tests/m36_qinghunpeng.js`（40 项）：玩法选项文案与高亮 / 清混碰目标含 pure·wind·pung 且绝不出现 std / `aiPlan`·`tileFitsPlan` 花色约束 / `aiClaim` 不吃不符花色·碰碰胡不吃顺子·有胡必和不受锁 / 敲麻模式行为不变 / 中发白算花（108~119 全为花、`PLAYCNT===31`）/ 圆心留空源码断言。
- 新增 `tests/m37_qhp_game.js`（17 项，对局级冒烟）：四家机器人真实跑 24 局清混碰——吃/碰/杠 0 次违反目标花色、吃进顺子全部同门、花牌（含中发白）从未被吃碰杠、**花色锁拦下 144 次旧逻辑乱吃**、终局听牌 52/96 家（机器人确实在做牌）；敲麻对照 12 局仍可自由吃碰、目标全是平胡。
- 新增 `tests/m38_qhp_ui.py`（端到端）：A 单机「玩法」行切换敲麻↔清混碰、`CFG.lajiHu` 与高亮联动；B 联机房主设清混碰→服务端白名单放行→玩家端同步；C 清混碰实战打满 2 局，有吃牌的家副露同门、副露全为碰/杠。
- `tests/m34_v1215_e2e.py` / `m35_center_layout.py` 圆心断言适配（「——」占位改为无人时留空）。
- 回归：Node 12 脚本全绿（unit 62 / m4 / m13 7 / m16 18 / m17 10 / m19 28 / m23 20 / m25 13 / m30 7 / m33 21 / m36 40 / m37 17）；Playwright 27 个脚本全过（m1~m35 全套 + m38）、0 JS 错误；extract 8/8（72/72），显式退出码 0；服务端启动日志同步 v1.2.16。

## v1.2.15（2026-09-02）

### 修复（3 项，本轮反馈）

1. **联机底部/顶部不显示听的牌（问题 1）**：根因是 `proto/server/view.js` 的 `projectFor()` 从不给客户端下发本人的敲定听牌——`applyView` 里 `knockWaits` 恒为空数组。修复：本人投影补发 `knockWaits`（别家仍不下发，听牌是私密信息），客户端接收端同步接上。联机实测：敲定后顶部「已敲 · 听 三條/六條」、底部「听牌中 → 三條 六條」均正常显示。
2. **点炮变自动胡 + 「自动胡牌」开关无效（问题 2）**：新增 `CFG.autoHu`（默认关）。关=自摸/点炮都出「胡/过」按钮自己决定（敲定后的点炮也从「默认自动胡」改为出「胡/过」）；开=能胡就自动胡（含敲定后「有和必和」）。设置面板新增「自动胡牌」开关行（`setCfg9`），服务端 `CFG_ALLOW`/`defaultCfg`/cfg 注入同步支持。另修复「自动敲」开时敲错座位 0 的问题（`knockCheck` 传 `p.idx`）。
3. **中途退出再回，听不了牌/点听牌没用（问题 3）**：两个根因都修了——
   - 服务端 `resumeSeat()` 重连时清掉该座位的 `noPromptSig`（掉线托管对敲听询问答过「不敲」后，同一听口引擎不再问，回来就永远敲不了）；
   - 同一座位被别处重新进入时，旧连接此前会静默留在牌桌却收不到任何询问/投影（「点听牌没用」的假死）。现在服务端 `rebind()` 会向旧连接发 `kicked` 并断开，客户端收到后回首页且不再自动重连，避免两个页面互抢座位。

### 调整：桌心布局（问题 4）

1. 圆上方「轮到 X」移除；改为**骰子 + 剩余牌数**一行（放不下骰子再缩小；豹子/开宝只高亮骰子、不再写文字；剩余牌数显示在骰子后面）。
2. 圆中间放**纯数字倒计时**（黄色高亮、无其他文字，≤3 秒变红闪烁）；单机/无决策时显示「——」占位。
3. 圆下方只放**最近手牌**：最多 3 手、最少 0 手，`fitLog()` 按实际可用空间动态测量决定行数。
4. **行重叠修复**：`.log` 子行加 `flex:0 0 13px` 禁止收缩——旧版空间不够时 flex 会把行高压扁导致两行字叠在一起（图 2 现象）；同时骰子行 30→26px、极矮屏 22px，360×640 小屏也能保住 2 行。

### 测试

- 新增 `tests/m33_autohu_waits.js`（21 项）：autoHu 开关全链路（默认值/设置面板/开=敲定后点炮自动胡不询问/关=出胡过按钮+漏胡记录）、`CFG.autoKnock` 敲对座位、投影只给本人下发 knockWaits、服务端 CFG_ALLOW/defaultCfg/resumeSeat 清 noPromptSig 源码断言。
- 新增 `tests/m34_v1215_e2e.py`：A 桌心新布局单机 6 项；A2 单机注入听牌手牌确定性验证「已敲·听/听牌中→」UI；B 联机 autoKnock+autoHu 回写、自然敲定后本人 knockWaits/顶部/底部听牌、圆心纯数字倒计时；C 牌局中「设置→回到首页」退出再进，同座位恢复、能继续应答服务端询问。
- 新增 `tests/m35_center_layout.py`：6 视口（360×640 ~ 1000×760）桌心布局回归——骰子行不压圆、剩余牌为数字、圆心纯数字/「——」、最近手牌 ≤3 行且行间不重叠、无纵向溢出。
- `tests/m6_clock.py` 适配新布局（改读 `#cdTxt` 圆心纯数字断言）；`tests/m2_full.py` 断言修正（起手花牌按规则暂存不补，按「首轮出牌时补齐 13/14 张」判定）。
- 回归：单测 62 / m16 18 / m17 10 / m19 28 / m23 20 / m25 13 / m30 7 / m33 21 全绿；m1/m2/m3/m5/m6/m7/m8/m9/m10/m11/m12/m13/m14/m15/m18/m20/m21/m24/m26/m27/m28/m29/m31/m32/m34/m35 Playwright 全过、0 JS 错误；extract 8/8（72/72），显式退出码 0。

## v1.2.14（2026-09-02）

### 调整（3 项，本轮反馈）

1. **手牌记录模块窗口级自适应**：`.log` 高度不再固定 39px，改由 `fitLog()` 按桌心可用高度算显示行数（**0~3 行**）并固定高度：
   - 同一窗口下：行数恒定，log 条数多少不挤占空间（多余行空间留在模块内），桌心圆零漂移（实测 dial.top 恒 337.8）
   - 窗口矮时：压缩为 2/1 行、甚至只显示「全部手牌记录」按钮（0 行，log 隐藏）；窗口高时回到 3 行
   - 实测：视口 600 → log 隐藏（仅按钮）；780 → 3 行（39px）；1000 → 3 行
2. **顶部「第 N 局」**：联机多局局数递增链路经专项验证正常——新增 `tests/m30_next_hand.js`（服务端 nextHand → handNo 递增、投影携带新局号）与 `tests/m32_gname_e2e.py`（真实联机打完一局→准备下一局→gname 变为「第 2 局」）。若走「结束本局→重开」流程，新一轮从第 1 局开始属正常设计。
3. **历史记录弹窗**：去掉顶部「本轮总分」列（保留各家当前分数）；列表每条显示「东风 第N 局」；**最近一局排在最上面**（倒序），点击查看仍对应正确的局。

### 修复（1 项）

1. **加杠提示「摸到 NaN」**：`ask('selfkong', ...)` payload 补 `tile`（摸牌=t / 出牌前=ko[0].tile）并加 `source: 'draw' | 'preDiscard'` 区分文案——摸牌路径「你摸到了 X，可以杠」、出牌前「你有 X，可以杠」。

### 测试

- 新增 `tests/m31_history_view.py`（历史弹窗去总分列/倒序/第N局/点击查看对应局）。
- 新增 `tests/m30_next_hand.js`（服务端多局 handNo）、`tests/m32_gname_e2e.py`（联机真实两局 gname）。
- `tests/m28_log_fixed.py` 扩展：同一窗口 log 高度恒定 + 桌心零漂移 + 窗口压缩（600→隐藏 / 1000→39px）。
- 回归：单测 62 / m16 18 / m17 10 / m19 28 / m23 20 / m25 13 / m30 7；m18、m20、m21、m24、m26、m27、m28、m29、m31 Playwright 全过、0 JS 错误；extract 8/8（72/72），显式退出码 0。

## v1.2.13（2026-09-01）

### 调整（3 项，本轮反馈）

1. **左右牌河高度自适应**：底部加自己名牌后，左右牌河（`rvE`/`rvW`）与上下牌河在短视口有挤压。新增 `fitRivers()`：按 `rowmid2` 可用高度算行数，行数钳制在 10~17 行之间（页面高→加高、矮→缩短），保证不与上下牌河重叠；`render()` 和 `resize` 监听都会重算。Playwright 实测：视口 600/780/1000 时 `rvE` 高度 179/269/305。
2. **选中同牌高亮**：出牌点选手牌时，台面（四家牌河）相同的牌加蓝色高亮框（`.hl`，`#3fb8ff`），区别于上张打出牌的红色框（`.last`）；`.hl` 定义在 `.last` 之后以保证同时选中时蓝色优先。
3. **点空白牌落回**：`renderHand` 的 `#hand` 容器绑定空白点击（target 为容器本身 / `drawgap` / `ting` 徽章时），取消 `SEL` 选中并重绘手牌 + 牌河，不触发出牌；切牌（点另一张）也调 `renderRiver()` 以同步高亮。

### 测试

- 新增 `tests/m27_river_hl.py`：选中后台面 `.hl` 数量 = 同牌张数；点空白后 `SEL=null`/`.hl=0`/手牌 `.sel=0`；视口 600/780/1000 → `rvE` 高度递增。
- 回归：单测 62 / m16 18 / m17 10 / m19 28 / m23 20 / m25 13 全绿；m18、m20、m21、m24、m26、m27 Playwright 全过、0 JS 错误；extract 8/8（72/72），显式退出码 0。

## v1.2.12（2026-09-01）

### 调整（6 项，本轮反馈）

1. **自己名牌（名字+局内总分）**：手牌下方（与北家对称位置）加 `nm0`，与对家/左右家对称显示局内总分；渲染走 `renderSeats`。Playwright 实测：nm0 居中、hand 下方，board/桌心圆/牌河零漂移。
2. **骰子旁封顶**：删除骰子旁的「封顶 X」标签——顶部算分模块（mCap）已有。
3. **加杠规则**：
   - `selfKongOptions` 改为扫描**整副手牌**（不再只看刚摸的牌），暗杠（4 张同）和加杠（已碰+手里第 4 张）都覆盖
   - **未敲真人**：摸牌后询问可杠；点"过"→ 下次摸牌/出牌前再次询问（每轮重置 `kongAsked`），实现"本轮不加，下一轮轮到出牌时再问一遍"
   - **已敲后**：摸到第 4 张必杠（自动 `applySelfKong`），牌尾补牌若胡牌算杠上开花（已有 `kongDraw` 上下文）
   - **抢杠胡**：加杠时若其他**已敲者**听该牌 → 抢杠胡，被抢者（加杠方）**付 3 倍**；多人抢杠按点炮多响处理；面板标注「加杠被抢」
   - **杠算花破无花果**：暗杠/明杠/加杠都在 `calcFlowers` 计入花数，无花果判定改为 `calcFlowers(p, null) === 0`（三处：extraFan / analyzeMe / playerDiFan），避免"无花果 + 杠"逻辑冲突
4. **豹子/开宝右上角去重**：删除 `huangfanTag` 的「豹子×2/开宝×2」标签，骰子旁已显示一次。
5. **历史记录去重**：`netHandle` 的 settle 帧提取 `netPushSettleRec(rec)`，按 `no + wind` 去重，防止断线重连时服务端补发 settle 造成同一局记录双份。
6. **已听牌底部文案锁定**：`renderHUD` 已敲时底部 `tingline` 恒显示 `p.knockWaits`（敲定时的听牌），不再随摸到/打出牌而变为「打X牌可以听→xx牌」。

### 测试

- `tests/m16_kong_fan.js` 更新断言：杠算花后无花果被破（这正是用户强调的结算要点）。
- 新增 `tests/m25_kong_rules.js`（13 项）：selfKongOptions 全手牌扫描 + discardTurn 出牌前询问 + kongAsked 防重 + 抢杠 3 倍 + 无花果被破。
- 新增 `tests/m26_self_nm_dedup.py`：自己名牌显示 + 历史去重（Playwright 注入）。
- 回归：单测 62 / m16 18 / m17 10 / m19 28 / m23 20 / m25 13；m18、m20、m21、m24、m26 Playwright 全绿、0 JS 错误；extract 8/8（72/72），显式退出码 0。

## v1.2.11（2026-09-01）

### 修复（3 项，本轮反馈）

1. **「结束本局」后重开仍发同一副牌**（联机）。根因：牌局种子只由房间号派生（`handSeed(room.no, 0, 0)`），重开房间号不变 → 同一副牌。现改为房间新增 `gameSeq` 开局计数，种子混入 `gameSeq`，每次开局都是新牌序（同一次开局仍可复现/复盘）。
2. **下一局坐庄规则修正**：只有一家胡牌 → **胡牌者坐庄**；多家胡牌（一炮多响）→ **点炮者坐庄**。自摸/八花齐/流局保持原规则（自摸胡家、流局庄家连庄）。
3. **顶部「番 / 单份」实时计算**：原实现把 `mFan`/`mScore` 的更新锁在「已敲且听牌」分支，未敲时固定显示 '—'，且从未使用本为顶部设计的 `playerDiFan`。现改为始终按 `analyzeMe` 实时口径显示——听牌取最佳听口番、未听取当前牌型番，单份金额同步（含豹子×2、荒番、封顶）。

### 新增：包三口规则（上海敲麻）

1. **承包判定**：本局内 A 对 B **吃三口或碰三口**（任意一种满 3 口），A、B 即互为「承包」关系（明杠不计入）。
2. **承包方自摸**：承包对方替三家支付并自加 2 份罚金，**共付 5 份**（另两家免付），胡牌者收 5 份。
3. **承包方点炮**：点炮者加罚 2 份，**共付 3 份**。
4. 其他两家点炮承包方 → **正常结算**（1 份）。
5. **豹子照常另算**：所有份额先按正常结算，再整体乘豹子/荒番倍率（走统一 `mult`，结算面板金额已含）。

### 测试

- `tests/m17_duoxiang_dealer.js` 扩到 10 项：新增单响场景（只有一家胡 → 下庄 = 胡牌者）。
- 新增 `tests/m23_chengbao.js`（20 项）：包三口基线对照 + 承包自摸 5 份 / 承包点炮 3 份 / 非承包正常 / 反向对称 / 吃三口 / 两碰不承包 / 分数守恒。
- 新增 `tests/m24_top_fan.py`：顶部番/单份开局即实时数字、0 JS 错误。
- 回归：单测 62 / m16 18 / m17 10 / m19 28 / m23 20 / m18、m20、m21、m24 Playwright 全绿；extract 8/8（72/72），显式校验退出码。

## v1.2.10（2026-09-01）

### 修复（2 项）

1. **起手补花 bug：花留在手里没补出去**（v1.2.8 引入）。`supplyPendingFlowers` 原用 `for` 固定次数循环只补"暂存区"的花，但补花从**牌尾摸上来的牌可能又是花**（花牌集中在牌尾，起手花越多越容易补到花），直接进了手牌没人再换。现改为循环补花：暂存花补完后继续从手里找花换牌尾，直到手里无花（与原版 `deal()` 的 while 语义一致）。
2. **extract 引擎抽取自 v1.2.9 起静默失败**：v1.2.9 记录脱敏改掉了 `doKnock` 里的 logMsg 行，extract 该条 patch 锚点失配直接 throw；发布命令里 `| tail` 管道掩盖了非零退出码，`&&` 链误判成功——**导致线上 v1.2.9 服务端引擎实际停留在 v1.2.8**（旧版补花逻辑 + 记录未脱敏，联机模式两者都没生效）。本版更新 patch 锚点并重新抽取同步引擎，脱敏与补花修复在联机端随本版一并真正生效。

### 测试

- `tests/m19_start_flower.js` 扩到 28 项：新增「chain」场景（牌尾补上来的又是花 → 必须循环换到手里无花、手牌张数守恒）；修复 `__cap.bahua` 跨场景残留导致的误报。回归：单测 62 / m16 18 / m17 6 / m18、m20、m21 Playwright 全绿；extract 8/8（72/72 导出），已改为显式校验退出码。

## v1.2.9（2026-09-01）

### 调整（1 项，本轮反馈）

1. **手牌记录脱敏**：「本局记录 / 底部记录条」不再泄露策略信息——敲牌只记「X 敲了」（不再记「听 5m/8m」等听什么牌），自摸只记「X 自摸！」（不再记「清一色」等目标牌型）。打出的牌、碰/吃/杠、补花等本就公开的信息照常记录；赢牌结算面板仍完整展示牌型（手牌此时已公开）。新增 `tests/m21_log_desensitize.py` 验证（浏览器内覆写 logMsg 捕获，敲牌两条路径记录均无「听」、无牌型）。

## v1.2.8（2026-09-01）

### 调整（1 项，本轮反馈）

1. **起手补花改到「轮到自己首次出牌时」才补**：原规则开局即瞬间补完所有人的起手花；现改为起手花暂存 `pendingFlowers`，轮到该玩家第一次出牌前统一补花（补完再出牌），符合上海敲麻"起手补花在轮到本人时"的玩法。起手阶段花牌以虚线 `.tile.pending` 样式在花区暂显，补花后归入正式花区并参与计分（无花果判定同步顺延），八花齐判定也顺延到补花后。新增 `tests/m19_start_flower.js` 验证（22/22）。

## v1.2.7（2026-09-01）

### 修复（3 项，本轮反馈）

1. **一炮多响后庄家**：一炮多响（双响/三响）结算后，下一局庄家改为**点炮者**（原为"点炮者下家最近的胡牌者"）。新增 `tests/m17_duoxiang_dealer.js` 验证（6/6）。
2. **中间牌桌被底部提示挤压漂移**：听牌行（`.tingline`）改为**固定占位 17px、靠 opacity 切换显隐**，不再用 height 收起占空间——底部提示有无时中间牌桌与桌心圆**零位移**（实测 0.00px 漂移）。
3. **已听仍显示"打xx牌 听牌 →"**：已敲听牌后，底部提示恒定显示「听牌中 → xx牌」，不再显示"打X牌 听牌 → X"（可敲提示由顶部 knockTag 兜底）。新增 `tests/m18_ting_hint.py` 验证。

## v1.2.6（2026-09-01）

### 修复（5 项，本轮反馈）

- **两侧牌河两排放得下、牌不再缩小**：撤销 v1.2.5 的牌河牌缩小（那反而让牌看着小），左右牌河（rv）加宽 40 → 48px 放下两列，高度定高 13 张并垂直居中——左右家视角的两排牌（13×2）完整均衡显示，上下牌河同步保证两行不裁
- **桌心「开宝」重复显示**：开宝时骰子旁显示「豹子 ×2 / 1·4 ×2」，旁边又挂一个「开宝 ×2」标签——同一信息显示两遍。已去掉重复标签，只保留骰子旁一处
- **没敲也显示「听牌 → xx」**：底部听牌行原来的判断只看牌型（差一张就听），不管敲没敲——没选敲也会出现「听牌中 → xx」误导。已改为**只在敲（听牌宣告）之后显示**；未敲时的可敲听提示仍由「可敲听 · 打X 听 Y」负责
- **暗杠中间两张牌消失**：暗杠的四张牌里中间两张被 CSS `visibility:hidden` 藏掉，只看到边上两张，像缺牌。已改为**四张全部显示**（整组米色牌背，符合暗杠扣牌的视觉惯例）
- **杠上开花番数疑问**：端到端引擎测试（新增 m16）真实跑了一局「敲定 → 杠 → 杠后补牌自摸 → 胡」，验证杠上开花 +1 正确进结算；并逐项核对全番种——门清 / 无花果 / 大吊车 / 杠上开花 / 海底捞月 / 天胡 +8 / 地胡 +8、底花（数牌明暗杠 1/2、风刻/风明暗杠 1/2/3）、点炮时门清/无花果照计、开宝 ×2 与荒番倍率、封顶截断，共 18 项断言全部通过

### 说明

- 若一局的番数很大，得分可能已到**封顶**（勒子上限）——此时再加番实际支付不变，结算明细里会显示「超过封顶，按勒子计」。如果遇到「杠上开花好像没加分」，先看明细里是否有这一行；仍怀疑有问题请提供对局截图核对
- 「抢杠」「承包」两项进阶规则按规则说明面板的标注，本版未实现（有意设计）

## v1.2.5（2026-09-01）

### 修复（5 项，本轮反馈）

- **左右牌河放不下两排牌**：上一轮把左右牌河（我视角两侧）宽度从 48 缩到 40px 后，左右两家（我的视角看是纵向）放不下两排牌。本轮改为**牌河宽度保持 40px、牌河牌整体缩小**（25×34 → 18×24），左右两排都能放满；上下（我与对家）的牌河同步加高（52 → 56px）给牌留足空间
- **去掉中央进度条**：桌子中间那个随剩余牌数变短的「进度条」已删除，只保留圆内的剩余牌数字，桌面更干净
- **等待页人数提示多算 1 人**：非房主已准备却仍显示「还有 1 位未准备」——房主被错误计入了「未准备」。已改为只统计**除房主外**未准备的人（房主不需要准备）
- **已听牌还提示「有 N 种听法可选」+ 敲听按钮**：听牌（敲）之后摸牌、以及自动敲牌模式下，不再重复出现听法提示和「敲听」按钮
- **重连后「查看结算」不弹窗**：本局结束后退出房间再重进，回到结束状态点「查看结算」没反应。已改为服务端缓存本局结算数据，断线重连时**补发 settle 消息**恢复客户端数据；重连收到 settle 不再自动弹窗（只恢复数据），底部「查看结算」按钮随时可点

### 配套改动（过程中发现）

- 服务端「全员离线自动中止」细化：**一局已结算（等准备下一局）时即使所有人都退出也不中止牌局**，结算数据保留等人重连回来（2 小时空闲回收兜底）；牌局进行中保持原有的 1.5 秒自动中止（回等待室、房间可重进）
- 客户端重连标志清理时机调整：重连中收到的 `welcome` 不再提前结束重连流程，改由随后到达的首个业务帧（牌面 / 结算）收尾，确保补发的结算消息走「不自动弹窗、只恢复数据」分支

## v1.2.4（2026-09-01）

### 界面微调（5 项）

- **结算牌缩小一档**：结算弹窗里的手牌/花/吃碰杠三排牌 30×41 → 25×34（明细行 21×28），弹窗内更宽松、不拥挤
- **左右牌河缩短**：左右两家的牌河宽度 48 → 40px，给上下（我与对家）的牌河腾出横向空间
- **桌心圆不再漂移**：开局未出牌与出牌后，圆的位置完全一致——桌心改为居中布局，「剩余时间」行固定 30px 高（没人在决策时显示「—」占位，不再塌陷/撑高）
- **听牌提示去重**：准备听牌状态下底部只保留一行提示（页面底部边框下方的「打 X 听牌 → Y」），提示区不再重复同一内容；单听法时提示区清空只留「敲听」按钮，多听法只显示「有 N 种听法可选」
- **封顶标签居中**：骰子右边的「封顶 N」与骰子垂直居中对齐（不再偏上/偏下）

### 新功能：等待页简易牌桌 + 自由换座

- 等待页的四个方格改为**简易牌桌**：中央一个迷你罗盘圆（东南西北四个方位块），上下左右四个位子
- **按本地视角排布**：我永远坐在下方（南），右家/对家/左家与进入牌局后的方位完全一致；圆上的方位字也跟着视角走（下方永远是我自己的风位，开局前按「座位 0 = 东」推算）
- **进房随机落座**：加入房间自动坐第一个空位；**点击任意空位即可换过去**（有人坐的位子点不了），换座后所有人立刻看到新的座位图
- 服务端新增 `moveSeat` 校验（只能在等待中换、目标必须为空），换座后准备状态重置
- **房主身份跟着人走**：房主换座不会被「座位号更小的人」顶替房主身份；房主退出后才顺延给首个在座者
- **修复：非房主看不到「还有 N 位未准备」实时变化**——房主/非房主统一同一套人数算法，服务端每次有人（取消）准备都广播全房间，四家看到的数字同步刷新

### 修复（回归）

- **`k is not defined`**：上一轮精简提示文案时删掉了 `const k = knocks[0]`，但单听法「敲听」按钮的回调还在引用它——联机点敲听直接崩。已改回 `knocks[0]`
- **`#app` 被嵌进首页容器**：等待页 HTML 整块替换时少写一个 `</div>`，游戏界面整个变成首页的子元素、首页一隐藏游戏界面就 0 尺寸不可见（多数回归测试因此挂掉）。已补齐闭合标签
- 管理测试脚本适配新牌桌结构（`rSeats` → `rBoard`）

## v1.2.3（2026-09-01）

### 界面微调（5 项）

- **罗盘指针缩小**：指针三角底边从 22px 缩到 16px（不再比方位块宽），同时方位块由 20px 微调到 30px（含内边距，与新指针比例更协调）；圆略缩到 88px（`min(124px,21vw)`），避免与东四字重叠
- **中央数字与东南西北字号协调**：中央剩余牌 `min(34,6vw)` → `min(28,4.8vw)`；东四字 `15px` → `17px`；断点（700/620/600/330）同步调整
- **顶部计分区瘦身**：删除计分区底下的「差多少牌听什么 / 听牌后的底和番」两行（fandetail），腾出的空间给游戏区；听牌时的「听牌中 → xx 牌」挪到页面底部边框下方（与「本局已结束」同区域），只在听牌时显示一行
- **骰子缩小**：骰子 21×21 → 17×17，骰子点 4.5×4.5 → 3.6×3.6，骰子盒 padding/gap 同步缩
- **最近几手牌不溢出**：log 行改单行省略（`text-overflow:ellipsis`），`logMsg` 从 3 条起逐减，**空间不够自动降到 2 / 1 条**，绝不超出 max-height；用户可点底部「全部手牌记录」看完整列表

## v1.2.2（2026-08-31）

### 界面调整

- **罗盘指针穿过圆边**：东南西北方位块贴到圆边（不再内移顶开内容），指针从方位块外侧直接戳出圆的边框（遮住一点圆边，z-index 在圆之上）
- **剩余牌数自适应**：桌心圆、剩余牌数字、方位块随窗口宽度平滑缩放（`min(140px,24vw)` 等），缩小窗口不再与东南西北重叠
- **操作按钮自适应**：按钮少则撑宽、多则均分；文案变长时自动缩小字号兜底，绝不换行（滚动条/窄屏下按钮协调）
- **听牌显示台面剩余张数**：敲听提示与「选择听法」面板里每张听牌标注剩余张数（4 − 自己手牌 − 三家牌河 − 吃碰杠副露，不算其他三家手牌）
- **准备状态四家可见**：结算后谁点了「准备下一局」，自己和其他三家都能在其牌河区看到「准备」字样（原来只有自己看得到）

### 修复

- **结算后切走页面再回来，底部按钮消失**：根因是断线重连时服务端 `resumeSeat` 会多发一条 `t:'start'`，客户端把它当「新一局」清空了 `NET.lastSettle`，导致 `G.result` 丢失、底部「回到首页/查看结算/准备下一局」渲染条件不成立。修复：重连不再发 `start`（view 足以恢复牌桌），且客户端结算按钮在联机时只依赖 `G.finished`（服务端 view 同步），不再强依赖 `lastSettle`

## v1.2.1（2026-08-31）

### 修复加杠报错 + 结算按钮稳健性

- **加杠报错**：联机时点「加杠」，服务端因网络传输丢失副露对象引用，`o.meld.type` 报 `TypeError`，导致牌局卡死、后续结算与底部按钮异常。现按牌重新定位「碰」的那组副露，修复该崩溃。
- **结算底部按钮**：结算后服务端可能补发一帧「空闲投影」把已结算状态短暂降级，导致底部「准备下一局 / 查看结算」按钮瞬间消失。现在拦截非开局阶段的降级，结算按钮稳定常驻。

## v1.2.0（2026-08-31）

### 1. 多人「准备下一局」流程（不再像单机直接开下一把）

- 一局结算后，弹窗按钮为「回到首页 + 准备下一局」；关闭弹窗后底部常驻「回到首页 / 查看结算 / 准备下一局」
- 点「准备下一局」后，**自己牌河区盖一个大「准备」字样**；再点可「取消准备」
- **四家都准备后自动进入下一局**（机器人/掉线者自动算已准备，不堵开局）

### 2. 不自动胡：满足条件显示「胡」按钮

- 自摸、点炮统一改为**显示「胡」按钮，点了才胡**；点「过」则弃胡继续打（机器人仍自动胡）

### 3. 听牌多选：选打哪张

- 敲听时如果打不同的牌会听不同的牌，弹出「选择听法」面板，列出「打 X → 听 Y」逐个选择

### 4. 暗杠 / 加杠

- 未敲定时摸到能杠的牌，显示「暗杠 / 加杠 / 过」按钮；敲定后仍保持「有杠必杠」自动杠

### 5. 吃碰杠优先级修正

- 分轮询问：**胡 > 杠=碰 > 吃**。先问所有能胡的人，没人胡再问杠/碰（杠与碰同级一起问），没人碰最后问下家吃

## v1.1.7（2026-08-31）

### 单机 claim 提示文案修正

单机游戏时 `NET.mySeat = -1`（未联机），导致 `remap(seat)` 偏移 +1，上家打牌询问时底部提示错误地显示成「**你** 打出 六条」之类。`remap` 改为：联机时正常按 `NET.mySeat` 换算；**单机直接返回原 seat**。

> v1.1.6 起，碰/吃/杠后罗盘指针已跟随动作人（`G.turn = p.idx`）。如果硬刷新浏览器（Ctrl+F5）后仍看到指针不跟人，请提供具体复现步骤再排查。

## v1.1.6（2026-08-31）

### 1. 吃碰杠的浮窗与指针指向修正

- **浮窗位置**：服务端下发的座位是「真实座位」，客户端此前直接用它定位，导致非房主碰牌时浮窗落到别人牌河。现在先按本地视角重映射，浮窗始终落在**当事人自己的牌河正中**。
- **罗盘指针**：碰 / 吃 / 杠之后，指针现在会**同步转向碰/吃/杠的人**（原来仍停在上一个出牌者身上）。

### 2. 听牌敲定一步到位

- 点「敲听」打出那张牌后，服务端再问「敲不敲」时**自动确认**，不再需要第二次点「敲定」。没点「敲听」、直接出牌触发的敲牌询问，仍保留「敲定 / 不敲」手动选择。

### 3. 吃牌显示具体牌型

- 有多种吃法时，按钮从统一的「吃」改为**「吃 345萬」「吃 456萬」**这种具体牌型，一眼分清吃的是哪一套。

## v1.1.5（2026-08-31）

### 1. 界面细节一轮收口

| 项 | 改动 |
|---|---|
| 首页标题 | 「上海敲麻」→「一道来敲麻」 |
| 按钮文案 | 所有按钮文字居中（首页单机/多人、房间页游戏设置/准备/查看设置/开始等） |
| 房间等待页 | 空位不再显示「开局补机器人」，只留「空」；有人未准备时「开始游戏」**置灰**而不是消失 |
| 名牌 | 去掉「底×番」，只留名字 + 分数（明细仍在结算面板） |
| 明牌提示 | 底部「明牌提示」按钮移除 |

### 2. 设置弹窗改为「提交制」

- 等待页**房主**：点选项先进入**草稿**（只预览、不生效），底部「取消 / 确定」；点「确定」才一次性提交到服务端
- 等待页**非房主**：只读，顶部文案「规则由房主设定，开局后锁定」，底部仅「关闭」
- 牌局中：规则锁定，仍保留「结束本局 / 关闭房间 / 回到首页」
- 房主提交后，其他玩家顶部弹「房主修改了游戏设置」

### 3. 倒计时更醒目、桌心不再漂移

- 「剩余时间」几个小字 + **金色大号秒数**，≤3 秒转橙红闪烁催促
- 桌心上方倒计时区固定高度，有/无倒计时时中间的圆不再上下跳

### 4. 吃碰杠浮窗落到当事人牌河区

- 吃 / 碰 / 杠 / 敲的提示从「屏幕顶部」改为**定位到当事人自己的牌河区域正中**（fixed 定位，不被牌河 overflow 裁掉）
- 罗盘指针放大（半宽 10px、伸出 14px），更显眼

## v1.1.4（2026-08-31）

### 1. 房间生命周期：一套统一的超时规则

原来散着三个互不相干的超时常量（`EMPTY_ROOM_TIMEOUT` / `IDLE_ROOM_TIMEOUT` / `IDLE_GAME_TIMEOUT`），行为很难预测。现在统一成一个阈值、一次扫描：

| 场景 | 行为 |
|---|---|
| **等待中**掉线 | 座位**保留 5 分钟**（宽限期内同昵称 + 房间号可直接坐回原位） |
| **等待中**最后一人掉线 | 房间**当场销毁** |
| **牌局中**全员离线 | 维持原样：1.5 秒后终止牌局，但**房间保留**，同昵称 + 房间号可回来接着打 |
| 空闲房间回收 | 三种情形**统一 2 小时**，一次扫描全部处理 |
| 扫描频率 | **1 小时**一次 |

牌局中只要牌局还在，掉线的人随时能顶替回来（不再受 60 秒托管显示阈值影响）。

### 2. 房主新能力

| 能力 | 说明 |
|---|---|
| **移出玩家** | 房间等待页，房主在每个其他玩家座位上看到「移出」按钮，解决有人掉线堵住开局的问题。牌局中拒绝移出（先结束本局） |
| **局内游戏设置** | 等待页房主点「游戏设置」，就是单机右上角那套规则（底 / 每花分值 / 勒子 / 速度 / 吃牌 / 七对 / 垃圾胡 / 自动敲牌）。改动实时广播给全房间 |
| **开局后锁定** | 进牌局后所有人只能**看**不能改（包括房主），设置项渲染成文本而非按钮 |

### 3. 修复：流局「幽灵弹窗」

单机或多人点设置里的「返回首页」后再开一把，会弹出上一局的流局结算弹窗。

根因：`backHome()` 把上一局挂起的 PEND `resolve(null)` 了，那个 async 主循环于是继续往下跑，跑完照常走 `finish({type:'liuju'})` —— 上一局的结算记到了新一局头上。

改法：`backHome()` **不再 resolve 旧的 PEND**，直接丢弃引用，让上一局的循环永远停在 `await` 处；同时在 `runHand()` 主循环及 `discardTurn` / `knockCheck` / `collectClaims` 之后共 **8 处**加了 `G.abort` 出口。

### 4. 界面增强

| 项 | 说明 |
|---|---|
| 房间号 | 下方增加「（点击数字复制）」提示，点数字即可复制 |
| 轮到谁出牌 | 东南西北圆四个方位文案**外侧增加向外指的三角指针**（随当前出牌者走，带呼吸动画 + 投影），像抽奖转盘的指针 |
| 吃 / 碰 / 杠 | 复用敲牌那套浮窗提示 |
| 浮窗停留 | 从一闪而过改为**停留 2 秒**再消失 |

### 5. 其他修复

- 设置弹层 `.mask` 的 z-index 50 → **70**，修复房间页被首页面板（z-index 60）遮挡导致点不到关闭按钮
- 房间等待页改设置时不再调用 `render()` —— 此时 `G.players` 还没初始化，会抛 `Cannot read properties of undefined (reading 'name')`
- `renderTop()` 增加防御：庄家信息在牌局未初始化时不渲染

### 验证

| 项 | 结果 |
|---|---|
| 房间规则设置（新增 `m8_room_rules.py`） | **17 项**全绿：房主改规则同步、非房主只读、开局后锁定、牌局中拒绝移出、等待页移出生效、非房主看不到移出按钮 |
| 界面增强（新增 `m9_ui.py`） | **20 项**全绿：复制提示 + 真能复制、罗盘指针只在当前方位且带动画、浮窗 1.6 秒仍在 / 2.6 秒消失、吃碰杠敲四种浮窗都在 |
| 引擎单元测试 | 62 通过 / 0 失败 |
| 200 局无头仿真 | 200/200 零和 |
| 纯 WebSocket 探针 | 5/5 通过 |
| 浏览器冒烟 m1 / 联机 m2 / 房主管理 m3 | 全绿 |
| 房间回收 m4 / 断线重连 m5 / 全局倒计时 m6 / 幽灵弹窗 m7 | 全绿 |
| JS 运行时错误 | 0 |

## v1.1.3（2026-08-31）

### 全局倒计时：桌心统一时钟

原来倒计时是「各端自己算」：服务端把截止时刻只发给当事人，他自己数秒；别人看不到，中途退出再回来还会和各家对不上。

现在改成**服务端权威、四家同源**：

| 项 | 说明 |
|---|---|
| 数据源 | 服务端在投影里下发「相对剩余毫秒」，各家按自己收到消息的时刻起算，不受手机时钟偏差影响 |
| 位置 | 东南西北圆**上方**显示「剩余时间：**N** 秒」（原「轮到 谁」这一行去掉） |
| 范围 | 只要有人在决策（出牌 / 吃碰杠 / 敲），四家同时看到同一个数；机器人行动时不显示 |
| 重连一致 | 中途退出、断线重连都只是重新取一次服务端状态，不再单独记自己的倒计时 |

底部提示行原来那个本地倒计时已移除，避免同源数据出现两份。

### 验证

| 项 | 结果 |
|---|---|
| 全局倒计时（新增 `m6_clock.py`） | 11 项断言全绿：四家秒数一致（13/13/13/13）、退出重进后仍一致（7/7/7/7）、不再显示「轮到谁」 |
| 引擎单元测试 | 62 通过 / 0 失败 |
| 4 端联机全流程 | 11 项断言全绿 |
| 房主管理 + 重进 | 27 项断言全绿 |
| 断线重连 | 13 项断言全绿 |
| 房间回收 | 6 项断言全绿 |
| 纯 WebSocket 探针 | 5/5 通过 |

## v1.1.2（2026-08-30）

补齐准备/开始规则、房间页显示、中途退出重进，以及浏览器挂后台后的断线重连。

### 准备与开始规则

| 项 | 规则 |
|---|---|
| 房主 | 不需要准备，房间页不显示「准备」按钮 |
| 开始条件 | 所有已加入的真人都准备后，房主才能点「开始对局」 |
| 空位 | **不看准备状态**，开局时自动补机器人 |
| 状态行 | 实时显示「还有 N 位玩家未准备」/「全员就绪！」 |

结束本局回到等待室后，准备状态会重置，需要重新准备再开下一局。

### 房间页显示

| 问题 | 处理 |
|---|---|
| 新房间空位显示「null 掉线」 | 服务端空座位一律下发 `null`，客户端渲染成「空 · 开局补机器人」 |
| 关闭房间后再开新房提示「连接已断开」 | 主动断开前先摘掉 `onclose/onerror`，避免把「自己关的」报成断线；进大厅时清空上一次的提示 |

### 牌局中途退出与重进

- 牌局进行中退出：座位保留、机器人托管，房间不销毁
- 想回来：用**相同昵称 + 同一房间号**加入，直接顶替回原座位继续打
- 一局结束后该座位释放，之后正常加入即可

### 断线自动重连（后台挂起恢复）

| 触发 | 行为 |
|---|---|
| 连接断开 | 退避重连（1.8 秒起、最长 6 秒，最多 8 次），界面提示「正在重连(N)」 |
| 回到前台 | 立即发 ping 探活；3 秒内无回应即判定半开连接，强制断开重连 |
| 重连被拒（房间已解散 / 凭证无效） | 停止重试，回首页并说明原因 |
| 断线时点「结束本局 / 关闭房间」 | 提示「连接已断开，正在重连…」并立即重连，不再点了没反应 |

设置面板「退出对局，返回首页」文案改为「回到首页」。

### 验证

| 项 | 结果 |
|---|---|
| 引擎单元测试 | 62 通过 / 0 失败 |
| 4 端联机全流程 | 11 项断言全绿，0 JS 错误 |
| 房主管理 + 准备规则 + 重进 | 27 项断言全绿（含空位显示、开始条件、牌局中退出重进），0 JS 错误 |
| 断线自动重连 | 13 项断言全绿（切断连接自动重连 / 浏览器离线后回前台恢复），0 JS 错误 |
| 房间回收 | 6 项断言全绿 |
| 纯 WebSocket 端到端探针 | 5/5 通过 |
| 单机冒烟 | 通过 |

## v1.1.1（2026-08-30）

补齐多人对战的房主管理与桌心显示问题。

### 房主功能

| 功能 | 位置 | 说明 |
|---|---|---|
| 结束本局 | 牌局中 · 设置面板（仅房主） | 中止当前牌局，所有人回到房间等待页，房间号不变，可重新准备再开 |
| 关闭房间 | 设置面板 / 房间等待页（仅房主） | 房间解散，所有人踢回首页，房间号立即失效 |

两个操作都有二次确认，避免误触。非房主看不到这两个按钮。

### 全员中途退出后可重进

牌局中若所有人都离线，服务端 1.5 秒后自动中止牌局、释放座位，房间回到等待状态：

- 房间号仍然有效，原班人马或新玩家都能用同一房间号重新进入
- 首位重新进入的人自动成为房主
- 无人留守的房间保留 10 分钟后自动释放

### 房间自动回收

房间只存在在服务端内存里，进程重启即全部清空。此外每 5 分钟扫一次，回收这三类：

| 情形 | 阈值 | 处理 |
|---|---|---|
| 无人留守的空房 | 10 分钟 | 关闭 |
| 等待中但长时间没人操作 | 2 小时 | 关闭 |
| 牌局挂起（不再推投影） | 3 小时 | 结束牌局并关闭 |

牌局正常推进时每次投影都会刷新活动时间，正常玩牌不会被误回收。每 30 分钟打印一次当前房间数。

### 桌心显示修复

| 问题 | 原因 | 处理 |
|---|---|---|
| 多人时桌心上下文字被遮住 | 左右两家的牌背 / 副露区随牌局推进不断变宽，把中央桌心挤窄；日志行又是 `nowrap` + 省略号，长昵称直接被截断 | 限制左右家区域最大宽度；日志改为自动折行、容器高度自适应；罗盘尺寸按屏高微调 |
| 单机正常、多人异常 | 单机名字固定为「你 / 下家 / 对家 / 上家」，多人用的是真实昵称，行长差异明显 | 同上，不再依赖名字长短 |

### 同期修复的隐藏问题

| 问题 | 说明 |
|---|---|
| 从房间号加入的人永远不是房主 | `join` 分支把 `isHost` 写死成 `false`。原房主退出后，剩下的人谁都开不了局 |
| 牌局中被踢回房间页 | 有人掉线时服务端会广播房间名单，客户端无条件切页。现在牌局中只更新状态、不打断牌桌 |
| 首页三个面板同时显示 | `.homeBox.hide` 缺 `display:none`，且根本没有切换到房间页的逻辑。已统一为一条 `.hide` 规则并补上 `showRoom()` |
| HTTPS 页面连不上服务器 | 浏览器禁止 HTTPS 页面用明文 `ws://`。新增 `wsUrl()`，HTTPS 自动升级 `wss://` |

### 验证

| 项 | 结果 |
|---|---|
| 引擎单元测试 | 62 通过 / 0 失败 |
| 200 局仿真 | 零和 200/200、同种子可复现、无异常、可见性裁剪全绿 |
| 4 端联机全流程 | 11 项断言全绿，0 JS 错误 |
| 房主管理 + 重进 | 15 项断言全绿（结束本局 / 关闭房间 / 全员退出后重进），0 JS 错误 |
| 单机冒烟 | 通过 |

## v1.1（2026-08-30）

新增**首页**与**多人联机对战**。单机玩法与 v1.0 完全一致。

### 首页

打开游戏先进入首页，两条入口：

| 入口 | 说明 |
|---|---|
| 单机游戏 | 直接进入原 v1.0 的一人对三家机器人，零网络依赖 |
| 多人对战 | 进大厅，填昵称 → 创建房间或输入 6 位房间号加入 |

多人大厅流程：建房拿到 6 位房间号 → 其他人输入房间号加入 → 四席到齐后各自点「准备」→ 房主点「开始对局」。人数不足时，空位由机器人补齐并自动托管。

### 多人对战

| 项 | 说明 |
|---|---|
| 房间 | 6 位房间号，最多 4 人；昵称与服务器地址自动记住 |
| 视角 | 自动按座位重映射，「我」永远在下方，右下左三家按真实方位 |
| 操作 | 出牌 / 敲听 / 吃碰杠胡 与单机完全一致；动作上报服务端裁决，非法动作被拒 |
| 倒计时 | 每次轮到你做决定时，提示行显示「剩 N 秒」；超时由机器人代打 |
| 掉线 | 60 秒内自动重连并续打；超时转机器人托管，界面标注「托管」 |
| 规则统一 | 由房间统一设定，联机时设置面板置灰，避免桌内规则分歧 |
| 结算 | 四端结算内容完全一致；结算面板与底部常驻「查看结算 / 下一局」 |
| 历史记录 | 联机时同步累计，可回看本轮每一局 |

### 架构

服务端权威：规则引擎只在服务端跑一份，客户端只渲染投影 + 上报动作。

- 引擎由 `proto/tools/extract.js` 从 `index.html` 机械抽取生成 `proto/server/engine.js`，禁止手改
- 可见性裁剪：每家只收到自己的手牌，其他家只给张数，牌墙只给剩余数
- 种子随机数：牌局可用房间号 + 局号 + 风圈复现
- 运行：`node server/server.js`，手机浏览器访问 `http://<电脑IP>:8080`

### 修复

| 问题 | 原因 | 处理 |
|---|---|---|
| 结算后无法开下一局 | 最后一次投影广播在结算前，`G.finished` 仍为 false，底部「下一局」不渲染；结算面板也直接用服务端原文、没有按钮 | settle 时应用结算帧投影并显式置 `finished`，面板统一走 `settleHtml()` 补按钮 |

### 验证

| 项 | 结果 |
|---|---|
| 引擎单元测试 | 62 通过 / 0 失败 |
| 200 局仿真 | 零和 200/200、同种子可复现、无死循环无异常、可见性裁剪 19 项断言全绿 |
| 4 端联机全流程 | 11 项断言全绿（座位、隐私、发牌、结算一致、下一局流转），0 JS 错误 |
| 纯 WebSocket 端到端探针 | 5/5 通过（建房→开局→两局结算） |

## v1.0（2026-08-30）

首个正式版本。单文件 HTML，上海敲麻单机版（1 真人 + 3 机器人）。

### 牌面与牌桌

| 项 | 说明 |
|---|---|
| 标准牌面 | 采用标准麻将矢量牌面（万/筒/索/风/中发白 + 春夏秋冬梅兰竹菊），万筒索风及中发白统一改为黑色线条 |
| 四人座位 | 按麻将桌四边布局，各自视角由近到远：手牌区 → 花+吃碰杠区 → 牌河区 |
| 左右家朝向 | 左右两家的牌河、花/吃碰杠排均按其自身视角排列；北家牌面朝向玩家 |
| 空槽占位 | 花+吃碰杠区即使无牌也保留占位，布局不随「有没有牌」跳动 |

### 规则

| 项 | 说明 |
|---|---|
| 番型 | 碰碰胡 1、混一色 1、混碰 2、清一色 3、清碰 4、风一色 4、风碰 8、七小对 2（可选）、垃圾胡 0 |
| 附加番 | 门清 / 无花果 / 大吊车 / 杠上开花 / 海底捞月 各 +1，地胡 / 天胡 +8 |
| 中发白算花 | 中、發、白每张计 1 花 |
| 算分 | 底 × 2^番 × 每花分值，封顶 N 勒 |
| 开宝 | 骰子豹子或 1·4 点 → 本局得分与封顶 ×2，桌心有高亮标识 |
| 荒番 | 流局后累积 ×2^荒番数，胡牌后 −1 |
| 敲麻规则 | 不敲不能胡、敲后锁定、有和必和、有杠必杠、漏胡 |

### 界面信息

| 项 | 说明 |
|---|---|
| 名牌 | 四家名字后显示 `底N×番N` 与 `N分`，实时计算 |
| 听牌标记 | 敲定后手牌/牌背上盖「听」圆章（与左右家牌背同宽，半透明），名牌不再显示「敲」 |
| 吃碰来源箭头 | 吃/碰/明杠牌组中间一张牌上显示箭头，指向打出该牌的那一家 |
| 桌心 | 剩余牌数、四家风位、骰子点面（豹子/1·4 高亮）、开宝/荒番/封顶标签 |
| 出牌记录 | 骰子下方显示最近三手，点「全部手牌记录」查看本局每一手 |
| 提示条 | 「建议打 X · 还差 N 步进听 → 目标 XXX」 |

### 结算与记录

| 项 | 说明 |
|---|---|
| 结算弹窗 | 放大展示胡牌牌面、番数拆解、各家分数变动 |
| 底部常驻按钮 | 关闭结算弹窗后，底部保留「查看结算」「下一局」 |
| 历史记录 | 右上角「记」查看本轮所有已结算牌局，可点任意一局回看完整结算 |
| 新一轮清零 | 历史面板内「开始新一轮」（二次确认）或设置里「重开一桌」才清空记录 |

### 其他

- 设置项：底、每花分值、封顶勒子、允许吃牌、七小对、垃圾胡、自动敲、机器人速度
- 单元测试 62 项，覆盖胡型/番型/向听/花数/听牌/牌墙发牌
