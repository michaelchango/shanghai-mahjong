# 上海敲麻 v1.1「好友开房间」—— 技术方案与准备工作

> 文档版本：v0.2（已拍板 + 地基已验证）
> 日期：2026-08-29
> 当前基线：v1.0（单文件本地 HTML，已定版，**本次准备工作中 index.html 未做任何改动**）
>
> **已确认的三个决策**：① 服务器权威架构 ② 跨互联网为主 ③ 4 真人开局 + 掉线自动接管

---

## 0. 结论先行

| 问题 | 结论 |
|---|---|
| v1.0 是纯本地 HTML 吗？ | **是**。零网络依赖，已逐行扫描验证（见 §1） |
| v1.1 要做联机，最大风险是什么？ | **不是写网络代码，是「隐藏信息」**——现在一个人的浏览器里装了全部四家的手牌和整副牌墙 |
| 推荐架构？ | **服务器权威**（服务端跑规则、客户端只管显示和点击），见 §3 |
| 现有规则代码要重写吗？ | **不用**。规则引擎是纯 JS、不碰 DOM，可以直接搬到服务端跑（已实测，见 §3.5） |
| 前端改动量？ | 集中在**两个切面**：「等玩家做动作」和「把牌画出来」，其余 UI 不动 |
| v1.0 代码现在要改吗？ | **先不动**。v1.0 保持可玩可回退（`index.html` 与 `v1.0` 标签逐字节一致） |
| 最难的一环验证了吗？ | **验证了**。规则引擎无头跑通 200 局、同种子可复现、可见性裁剪 19 项断言全绿（见 §3.5） |

---

## 1. v1.0 网络依赖扫描结果（回答 Q1）

对 `index.html`（2272 行 / 265 KB）全量扫描：

| 检查项 | 扫描命令命中 | 结果 |
|---|---|---|
| `http://` / `https://` / `ws://` URL | `grep -nEi "https?://\|wss?://"` | **0 处** |
| 网络 API（`fetch` / `XMLHttpRequest` / `WebSocket` / `EventSource` / `sendBeacon`） | `grep -nE "\b(fetch\|XMLHttpRequest\|WebSocket\|EventSource\|sendBeacon)\b"` | **0 处** |
| 外链资源（`<link>` / `<script src>` / `<img>` / `@import` / `url(http...)`） | `grep -nEi "<(link\|img\|script)[^>]*(src\|href)=\|@import\|url\(['\"]?http"` | **0 处** |
| Web Worker / 动态 `import()` | `grep -nE "new Worker\|import("` | **0 处** |

**唯一一处「存储」**（第 847 / 849 行）：

```js
try{ const s = JSON.parse(localStorage.getItem('sjm_cfg') || '{}'); Object.assign(CFG, s); }catch(e){}
function saveCfg(){ try{ localStorage.setItem('sjm_cfg', JSON.stringify(CFG)); }catch(e){} }
```

这是**手机浏览器本地存档**，只存设置项（底分/单位/花牌规则等），**不联网、不上传**。

牌面素材也已全部内联（`STD_FACES` 对象，约 166 KB 的 SVG），没有任何图片文件需要下载。

> ✅ **结论：飞行模式、无 SIM 卡、断网都能玩。把 `index.html` 传到手机，用浏览器打开即可。**

---

## 2. 现有代码架构盘点（联机改造的地图）

### 2.1 全局状态

```js
const G = {
  players: [], wall: [], wpos: 0, wtail: 145,   // ← 牌墙与位置：联机后必须留在服务端
  dealer: 0, turn: 0, handNo: 1, roundWind: 0,
  huangfan: 0, kaibao: false,
  lastDiscard: null, lastFrom: -1, lastDiscardEl: null,
  running: false, logs: [], totalHands: 16,
  finished: false, result: null, history: []
};
```

每个玩家对象 `mkPlayer()` 含：`hand / flowers / melds / discards / knocked / knockWaits / menqing / drawn / score ...`

### 2.2 主流程：一个 async 循环跑完整局

```
runHand()                     // 洗牌 → 掷骰 → 发牌 → 补花
  ├─ knockCheck(p)            // 是否敲牌
  ├─ discardTurn(p)           // 摸牌 → 出牌
  │    └─ ask('discard')      // ★ 等待人类点击
  └─ collectClaims(tile,from) // 询问其他三家吃/碰/杠/胡
       └─ ask('claim', {...}) // ★ 等待人类选择
```

**关键设计（好消息）**：所有「等人做决定」的地方都收敛在**一个函数**里：

```js
let PEND = null;
function ask(kind, payload){
  return new Promise(res => { PEND = { kind, payload, res }; render(); });
}
function resolvePend(v){
  if (!PEND) return;
  const r = PEND.res; PEND = null;
  ... r(v);   // 解开 Promise，主循环继续
}
```

> 这是整个改造的**黄金切点**。只要把 `ask()` 内部改成「本地玩家 → 弹按钮；远端玩家 → 发网络消息等回包」，主循环一行都不用动。

### 2.3 AI 决策（三处）

| 函数 | 行号 | 作用 |
|---|---|---|
| `aiTargetOf(p)` | 974 | 机器人定「做哪种牌型」的目标 |
| `aiChooseDiscard(p)` | 996 | 机器人选打哪张 |
| `aiClaim(p, tile, from, opts, isNext)` | 1025 | 机器人决定吃/碰/杠/胡 |

联机后这三个函数**留在服务端**，用来给空位补机器人（2 真 2 假、3 真 1 假都能开）。

### 2.4 随机数（四处）

| 位置 | 行号 | 用途 |
|---|---|---|
| `shuffle(a)` | 547 | 洗牌（`Math.random()`） |
| `buildWall()` | 880 | 生成 145 张牌墙 |
| `deal()` | 1643 | 发牌 + 补花 |
| `runHand()` | 1776 | 掷骰子（含开宝判定） |

联机后**全部搬到服务端**，并建议换成**带种子的伪随机**（同一个种子必然洗出同一副牌）。好处：可复盘、可验证、服务端崩了能重建牌局。

### 2.5 视角硬编码（需要清理的坑）

「我」现在被**写死成 seat 0**，共 11 处：

```
1155 renderHand   1322 renderMe    1378 playerDiFan  1449 renderHUD
1451 renderHUD    1564 renderActs  1570 renderActs   1600 renderActs
1614 renderActs   1621 showTip     1684 doKnock
```

联机后需要引入 `MY_SEAT`（我坐哪个位置由房间分配），把这 11 处 `G.players[0]` 全换成 `G.players[MY_SEAT]`。

### 2.6 渲染层

```js
function render(){
  renderTop(); renderHUD(); renderSeats(); renderRiver();
  renderMe(); renderHand(); renderActs(); fitBacks();
}
```

7 个 `render*()` **都是直接读 `G`** 的。联机后它们要改成读「服务端下发的可见投影」（见 §4.3）。

---

## 3. 架构选型：三条路线对比

| 维度 | **A. 服务器权威**（推荐） | B. 房主权威 + P2P 直连 | C. 混合（P2P 优先，失败走服务器） |
|---|---|---|---|
| **一句话** | 一台小服务器当裁判，四台手机都是「显示器+遥控器」 | 房主的手机当裁判，其他三家直连房主 | 能直连就直连，直连不上走中转 |
| 是否要服务器 | 需要（很小，Node 一个进程） | 只在「找人」那一下需要一个信令服务 | 两种都要 |
| **防作弊** | ✅ 强：谁也看不到别人的牌 | ⚠️ 房主浏览器里有全部手牌，靠人品 | ✅ 强 |
| 规则代码改动 | **零**（搬到 Node 直接跑） | 零 | 零 |
| **断线重连** | ✅ 支持，重连发快照 | ❌ 房主掉线整局崩 | ✅ 支持 |
| 跨互联网（各自在家） | ✅ 只要有公网地址 | ⚠️ 需要 STUN/TURN，移动网络下成功率不保证 | ✅ |
| 局域网（同一 WiFi） | ⚠️ 也要连服务器（除非本地跑） | ✅ 天然适合 | ✅ |
| 观战 / 战绩 / 复盘 | ✅ 顺理成章 | ❌ 很难 | ✅ |
| **工作量** | 中 | **中高**（NAT 穿透 + 重连是深坑） | 高（两套都要写） |
| 长期维护 | 简单 | 麻烦（各种网络环境踩坑） | 最麻烦 |

### 推荐 A 的三个理由

1. **规则代码一行不用改。** 现有规则引擎（`evalAll` / `shantenStd` / `claimOptions` / `scoreOf` …）是纯 JS、不碰 DOM —— 单测脚本能把它从 HTML 里抽出来独立跑 62 项断言，这就证明了它能在 Node 里跑。这是最大的一块工作量，直接省掉。

2. **手机网络环境容不下 P2P。** 你要在手机上玩，国内移动网络普遍是 NAT444 / 运营商级 NAT，双向打洞成功率很不好看。P2P 一旦失败就要退回 TURN 中转 —— 那还是得有服务器，等于白绕一圈。

3. **「局域网」的需求，A 也能满足。** 好友面对面时，在任意一台电脑上跑服务端，其他人连 `192.168.x.x` 就行；或者干脆部署到公网，在哪都能开。

> 💡 **一句话给非技术视角**：A 是「建个微信群聊，服务器就是群主」；B 是「四个人互相加微信点对点，谁建的群谁当裁判，群主退群群就散了」。

**✅ 你的选择：A 服务器权威 + 跨互联网为主。**

---

## 3.5 地基验证：规则引擎可移植性已实测通过

方案能不能成立，全押在一个假设上：**index.html 里的规则引擎能不能原封不动搬到 Node 服务端跑**。
这条如果不成立，「规则代码零改动」就是空话，整个工期要翻好几倍。所以先把它验证了。

做法：写脚本从 `index.html` 抽出规则引擎 → 塞进 Node 无头环境（DOM 用替身接住）→ 让四个 AI 自动打完整局。

```bash
$ node proto/tools/extract.js     # 从 index.html 抽取引擎
$ node proto/server/simulate.js 200
```

**结果：**

| 验证项 | 结果 |
|---|---|
| 引擎可在 Node 无头运行 | ✅ 200/200 局完整打完并结算 |
| 同种子可复现 | ✅ 种子 `3716381449` 跑两次，分数 `[-8,-8,24,-8]` 与 40 条出牌记录逐条一致 |
| 规则自洽（零和校验） | ✅ 200/200 局四家分数合计为 0 |
| 无死循环 / 无异常 | ✅ 0 局异常，最多一手 83 步 |
| 引擎单局耗时 | 约 350ms（含全部 AI 决策，真实开销可忽略） |
| **可见性裁剪** | ✅ **19 项断言全绿**（见下） |
| 单份投影数据量 | **1.3 KB**（手机流量无忧，且每局只发一次快照） |

**可见性裁剪实测**（这是防作弊的关键）：

```
✅ seat0~3 都只能看到自己的手牌，其他三家 hand 为 null
✅ seat0~3 都知道他人手牌张数（10/13/13 …，画牌背要用）
✅ seat0~3 只拿到牌墙剩余张数，牌墙内容从未出现在下发数据里
✅ 四家看到的公开信息（花牌/副露/牌河/分数/是否敲牌）完全一致
```

> **一句话结论**：最难的两件事（规则复用、信息隔离）都已经跑通验证，v1.1 剩下的工作量主要在「联网管道 + 渲染层改读投影」，是可以按部就班推进的确定性工作。

---

## 4. 服务器权威方案设计

### 4.1 分层

```
┌─────────────────────────────────────────────┐
│  手机浏览器 ×4                                │
│  ┌──────────┬──────────┬─────────────────┐  │
│  │ 渲染层    │ 输入层    │ 网络层           │  │
│  │ render*() │ ask()分流 │ WebSocket 收发   │  │
│  └──────────┴──────────┴─────────────────┘  │
│        ↑ 只能看到「自己的手牌 + 公共信息」      │
└────────────────────┬────────────────────────┘
                     │  WebSocket (JSON)
┌────────────────────┴────────────────────────┐
│  服务端（Node + ws）                          │
│  ┌───────────────────────────────────────┐  │
│  │ 房间管理（建房/加入/座位/准备/开局）     │  │
│  │ 规则引擎 ← 直接复用 index.html 里的代码 │  │
│  │ runHand() 主循环 + ask() 改成等人回包   │  │
│  │ 可见性裁剪（每家只看该看的）             │  │
│  │ AI 补位（真人不够时填机器人）            │  │
│  └───────────────────────────────────────┘  │
└─────────────────────────────────────────────┘
```

### 4.2 改造点清单（这是实施时真正要动的）

| # | 位置 | 现状 | 改成 | 难度 |
|---|---|---|---|---|
| 1 | `ask()` L1064 | 本地 Promise 等点击 | 分流：本家 → 弹按钮；他家 → 发 `ASK` 消息等回包 | 低 |
| 2 | `resolvePend()` L1067 | 直接解 Promise | 本家点击 → 顺便发 `ACT` 消息给服务端 | 低 |
| 3 | `runHand()` L1760 | 每个客户端都跑 | **只在服务端跑**；客户端删掉，改事件驱动 | 中 |
| 4 | `G.players[i].hand` | 四家手牌全在本地 | 客户端只存自己的；其他家存 `handCount` 数字 | 中 |
| 5 | `G.wall` | 整副牌墙在本地 | 客户端只存 `wallLeft` 剩余张数 | 低 |
| 6 | `shuffle()` / 骰子 L547/1776 | `Math.random()` | 服务端用**带种子 RNG**，结果下发 | 低 |
| 7 | 11 处 `G.players[0]` | 「我」写死 seat 0 | 引入 `MY_SEAT` 变量全部替换 | 低 |
| 8 | 7 个 `render*()` | 直接读 `G` | 改读 `VIEW`（服务端下发的可见投影） | **中高** |
| 9 | `CFG` 设置 | 各自 localStorage | 房主设置 → 开局时下发并锁定，全员一致 | 低 |
| 10 | `aiChooseDiscard` 等 | 本地跑 | 留在服务端，用于空位补机器人 | 低 |
| 11 | 出牌超时 | 无（单机不着急） | 服务端加倒计时，超时自动打（托管） | 低 |
| 12 | 断线重连 | 无 | 快照 + 增量序号（见 §4.4） | 中 |

> **第 8 项（渲染层）是唯一的大工程**，其余都是小切口。

### 4.3 可见性裁剪：谁能看到什么

服务端给每个座位下发**专属投影**：

| 数据 | 自己 | 其他三家 | 结算后 |
|---|---|---|---|
| 我的手牌 `hand[]` | ✅ 全部 | ❌ 只见张数 | ✅ 亮牌 |
| 花牌 `flowers[]` | ✅ | ✅ 公开 | ✅ |
| 吃碰杠 `melds[]` | ✅ | ✅ 公开 | ✅ |
| 牌河 `discards[]` | ✅ | ✅ 公开 | ✅ |
| 是否敲牌 `knocked` | ✅ | ✅ 公开 | ✅ |
| 牌墙 `wall[]` | ❌ | ❌ | ❌（只见剩余张数） |
| AI 的「做牌目标」`target` | — | ❌ 永不外发 | ❌ |

> 服务端永远不下发 `wall[]` 和别人的 `hand[]`。这样即使用户打开浏览器 F12 也偷看不了牌。

### 4.4 通信协议草案

传输：**WebSocket + JSON**（手机友好、穿透 443、可复用 HTTPS 基础设施）。

**客户端 → 服务端**

| 消息 | 载荷 | 时机 |
|---|---|---|
| `join` | `{ roomNo, name }` | 输房间号进入 |
| `create` | `{ name, cfg }` | 房主建房 |
| `ready` | `{ ready: true }` | 点「准备」 |
| `start` | `{}` | 房主开局 |
| `act` | `{ kind:'discard'\|'claim'\|'knock', payload, seq }` | 响应服务端的 `ask` |
| `chat` | `{ text }` | 聊天/表情 |
| `reconnect` | `{ roomNo, token, lastSeq }` | 断线重连 |
| `ping` | `{}` | 心跳（30s） |

**服务端 → 客户端**

| 消息 | 载荷 | 说明 |
|---|---|---|
| `welcome` | `{ seat, mySeat, token }` | 进房成功，分配座位 |
| `room` | `{ players:[{seat,name,ready,online}], cfg }` | 房间状态变化 |
| `start` | `{ dealer, roundWind, handNo, dice }` | 开局 |
| `ask` | `{ kind, payload, deadline }` | **轮到你做决定**，带倒计时 |
| `event` | `{ seq, kind, data }` | 牌局事件（见下表） |
| `state` | `{ view, seq }` | 完整快照（重连时下发） |
| `settle` | `{ html, deltas, scores }` | 结算 |
| `error` | `{ code, msg }` | 出错 |

**牌局事件 `event.kind`**：`draw`（摸牌，只给本人发具体牌）/ `discard` / `meld`（吃碰杠）/ `knock`（敲）/ `kong` / `hu` / `liuju`（流局）/ `turn`（轮到谁）

**重连机制**：每个 `event` 带自增 `seq`；客户端缓存最后收到的 `seq`。重连时发 `lastSeq`，服务端补发缺失的增量；缺口太大就下发完整 `state` 快照。

### 4.5 技术栈建议

| 层 | 选型 | 理由 |
|---|---|---|
| 服务端 | **Node.js 22 + `ws`** | 规则引擎是 JS，直接复用；`ws` 是最轻的 WebSocket 库 |
| 部署 | 腾讯云开发 CloudBase 云托管 / 轻量应用服务器 | 你已有 CloudBase 使用经验，部署路径熟 |
| 前端 | 保持单文件 HTML + 一个可拔插的 `net.js` 联网模块 | v1.0 单机玩法不受影响 |
| 房间号 | 6 位数字短码 | 手机好输入，比二维码省事（二维码可后续加） |

**关于「要不要拆文件」**：建议**保持 `index.html` 单文件不变**，联网功能放在**独立的 `net.js` + `server.js`** 里。打开方式区分：

- 单机：直接打开 `index.html`（和现在完全一样）
- 联机：打开带 `?room=123456` 参数的地址，或从首页点「开房间」

这样 v1.0 的自包含优势不丢，出问题时一条路径回退。

---

## 5. 里程碑拆分（建议）

| 版本 | 目标 | 关键交付 | 验收标准 |
|---|---|---|---|
| **v1.1** | **房间核心跑通** | 建房/加入（6 位房间号）/座位/准备/开局；**4 真人**完整打完一局并结算；**掉线自动接管** | 4 台设备开一局，全程无状态错乱；断网 30 秒能重连续上；掉线超 60 秒机器人接管打完本局 |
| **v1.2** | **体验补齐** | 出牌倒计时+超时托管、聊天表情、房间内观战席、战绩本地留存 | 中途关掉一台手机，其余三家不卡死、不等待 |
| **v1.3** | **完整化** | 邀请链接/二维码、战绩云端持久化、复盘（用种子 RNG 重放整局） | 换手机也能看到历史战绩；能回看任意一局的每一手 |

> 按你的选择，**v1.1 只做 4 真人开局**，不混合 AI（AI 只用于掉线接管）。先把「同步」这件事做对——从开局就补位 AI 是后面加一行分支的事，但会让 v1.1 的验证面无谓变大。

---

## 6. 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| 渲染层改造（第 8 项）范围扩散 | 工期 longest | 先做「投影函数」`projectFor(G, seat)`，让 `render*()` 从读 `G` 改成读 `projectFor(G, MY_SEAT)`，接口形状保持一致，改动可控 |
| 网络延迟让节奏变慢 | 体验 | 服务端本地跑 AI 无延迟；人类出牌倒计时 15s，超时自动打；动画时长按 `CFG.speed` 同步 |
| 服务器成本/运维 | 长期 | 4 人房是极小负载，一台 1 核 1G 就够；CloudBase 云托管按量付费，好友局几乎不花钱 |
| 作弊（开两个号看牌） | 体验 | 短期靠房间号 + 熟人局；长期加登录 + 战绩追溯 |
| 规则被改动导致单机/联机不一致 | 维护 | **规则引擎只有一份**（服务端那份），单机版从同一份代码生成，禁止两边各写一份 |

---

## 7. 已确认的决策

| # | 决策项 | 你的选择 | 带来的实现约束 |
|---|---|---|---|
| 1 | 架构路线 | **服务器权威** | 服务端跑规则 + 发牌；客户端只渲染 + 上报动作；需要一台 Node 服务器 |
| 2 | 使用场景 | **跨互联网为主** | 服务端部署到公网（CloudBase 云托管 / 轻量服务器）；局域网场景通过连同一公网地址覆盖，不做本地服务发现 |
| 3 | 开局人数 | **4 真人 + 掉线自动接管** | v1.1 不混合 AI；AI 只在掉线超时后接管，接管后本局打完，原玩家重连可收回控制权 |

**决策带来的两个具体设计**：

1. **掉线接管规则**（v1.1）：掉线 30 秒内保留座位并自动重连；超过 60 秒未重连，机器人接管出牌并在界面标注「托管中」；该玩家重连后可立即收回控制权，下一局恢复正常。
2. **不做本地服务发现**：跨互联网为主，就不去折腾 mDNS/局域网广播这类东西，省掉一块不确定工作量。好友面对面时同样连公网地址即可（流量开销见 §3.5，每局快照仅 1.3 KB）。

---

## 8. 本次准备工作产出物

**v1.0 的游戏本体一行未动**（`git diff v1.0 -- index.html` 无输出，随时可回退）。新增的全部在 `proto/` 与本文档：

| 文件 | 作用 | 状态 |
|---|---|---|
| `proto/tools/extract.js` | 从 `index.html` 抽取规则引擎 → `engine.js`。**保证规则只有一份**，杜绝「单机一套、联机一套」 | 可用 |
| `proto/server/engine.js` | 抽出的引擎（238 KB，自动生成，**禁止手改**） | 已验证 |
| `proto/server/rng.js` | 种子随机数（mulberry32）+ 房间号/局号派生种子 | 已验证 |
| `proto/server/headless.js` | 无头运行环境：DOM 替身 + 载入引擎，让浏览器代码能在 Node 里跑 | 已验证 |
| `proto/server/view.js` | **可见性裁剪**：`projectFor(S, seat)` 生成每家专属投影；`projectReveal(S)` 结算亮牌 | 已验证 |
| `proto/server/simulate.js` | 验证脚本：单局演示 / 同种子复现 / N 局压力 / 可见性裁剪 | 已验证 |
| `联网方案-v1.1-准备工作.md` | 本文档 | — |

**复现验证只需两条命令：**

```bash
node proto/tools/extract.js          # 1. 从 index.html 抽引擎
node proto/server/simulate.js 200    # 2. 跑 200 局 + 可见性裁剪验证
```

> ⚠️ `proto/` 是**架构验证原型**，不是 v1.1 的成品服务端。v1.1 正式开工时，`view.js` / `rng.js` 会直接沿用，`engine.js` 继续由脚本生成，真正的房间管理、WebSocket 管道、超时托管还需要新写。

---

## 附：v1.0 交付物清单（基线，未改动）

| 文件 | 说明 |
|---|---|
| `index.html` | 单文件游戏，265 KB，零外部依赖 |
| `README.md` | 玩法、规则表、界面说明 |
| `CHANGELOG.md` | v1.0 功能清单 |
| git | commit `3cd5eaa`，tag `v1.0`（未推送远端，等你决定何时推） |

> 按你的要求，本次准备工作**没有执行任何 git 提交**。上述新增文件都还是未跟踪状态，你可以在确认后按版本节奏自行提交。
