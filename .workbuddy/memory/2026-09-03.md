# 2026-09-03

## 初始化 git 并推送到 GitHub
- 目录原本不是 git 仓库，已 `git init -b main`，初始提交 `6a88f4c`（59 个文件，含 index.html / proto / server / tests，忽略 node_modules 与 proto/server/engine.js）。
- 远程 `origin` = `https://github.com/michaelchango/shanghai-mahjong.git`，已 `git push -u origin main` 成功推上 GitHub（通过已连接的 GitHub 连接器注入凭据；`gh` CLI 本身未登录但 git push 可用）。
- 沙箱里 `refs/remotes/origin/main` 跟踪引用不会自动持久化，需手动 `mkdir -p .git/refs/remotes/origin && git rev-parse HEAD > .git/refs/remotes/origin/main` 才能让 `git status` 显示 in-sync（0 ahead/0 behind）。
